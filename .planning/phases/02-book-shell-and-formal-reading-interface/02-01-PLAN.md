---
phase: 02-book-shell-and-formal-reading-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/data/book-spine.ts
  - site/src/data/book-spine.test.ts
  - site/astro.config.mjs
  - site/src/pages/corpus/[slug].astro
  - site/src/components/SourceLinkPanel.astro
  - site/src/components/BookFooterNav.astro
  - site/src/styles/atlas.css
autonomous: true
requirements: [BOOK-02, BOOK-03, BOOK-05, BOOK-06, DESIGN-01, DESIGN-04]
must_haves:
  truths:
    - "D-05: Reader can navigate a stable book spine from overview to umbrella framework to all twelve pillars."
    - "D-07: Reader can open umbrella and pillar source-detail pages with summary, source-status treatment, canonical PDF/source/bibliography links, and Phase 3-ready lightweight structure."
    - "D-08: Reader can move to previous and next spine entries using subtle footer navigation while the sidebar remains primary."
    - "D-06: Reader sees the same explicit conceptual-arc order in sidebar and previous/next navigation, never inventory or alphabetical order."
    - "Reader can distinguish material-kind treatment for exactly canonical, supporting, synthesis/review, and archived/provenance material, alongside source status labels."
  artifacts:
    - path: "site/src/data/book-spine.ts"
      provides: "Single conceptual-arc spine, Starlight sidebar data, and previous/next helpers"
      exports: ["pillarArc", "bookSpine", "bookSidebar", "getPreviousNext"]
    - path: "site/src/pages/corpus/[slug].astro"
      provides: "Generated static source-detail pages for umbrella and all twelve pillars"
      exports: ["getStaticPaths"]
    - path: "site/src/components/SourceLinkPanel.astro"
      provides: "Canonical source trail link panel with visible material-kind treatment rendered from CorpusEntry metadata"
    - path: "site/src/components/BookFooterNav.astro"
      provides: "Subtle previous/next footer navigation from the shared spine"
    - path: "site/src/styles/atlas.css"
      provides: "Phase 2 atlas tokens, focus states, source panels, and navigation styling"
  key_links:
    - from: "site/astro.config.mjs"
      to: "site/src/data/book-spine.ts"
      via: "bookSidebar import"
      pattern: "bookSidebar"
    - from: "site/src/pages/corpus/[slug].astro"
      to: "site/src/data/corpus.ts"
      via: "getStaticPaths over corpusEntries"
      pattern: "corpusEntries\.map"
    - from: "site/src/components/BookFooterNav.astro"
      to: "site/src/data/book-spine.ts"
      via: "getPreviousNext(currentId)"
      pattern: "getPreviousNext"
---

## Phase Goal

**As a** reader, **I want to** use a polished, accessible, book-like interface with stable navigation and consistent formal-content treatment, **so that** I can study the harness architecture corpus without losing source traceability.

<objective>
Create the data-driven book spine and generated source-detail page slice for Phase 2.

Purpose: The conceptual-arc order is the backbone for sidebar navigation, homepage ordering, generated source-detail pages, and footer previous/next links. Building it first prevents navigation drift and source-link duplication.
Output: Shared spine module, tests, sidebar configuration, static source-detail route, source link panel, footer nav component, and base atlas stylesheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-book-shell-and-formal-reading-interface/02-CONTEXT.md
@.planning/phases/02-book-shell-and-formal-reading-interface/02-RESEARCH.md
@.planning/phases/02-book-shell-and-formal-reading-interface/02-PATTERNS.md
@.planning/phases/02-book-shell-and-formal-reading-interface/02-UI-SPEC.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-01-SUMMARY.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-02-SUMMARY.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-03-SUMMARY.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-04-SUMMARY.md
@site/package.json
@site/astro.config.mjs
@site/src/data/corpus.ts
@site/src/data/corpus.schema.ts
@site/src/data/corpus.test.ts
@site/src/pages/inventory.astro
@site/src/styles/phase-1.css

<interfaces>
From site/src/data/corpus.schema.ts:
- sourceStatusLabels maps canonical to "Canonical", missing-source to "Missing source", archive-blocked to "Archive blocked", provenance-only to "Provenance-only".
- CorpusEntry includes id, kind, title, slug, summary, canonicalTex, canonicalPdf, optional bibliography, and sourceStatus.

Required Phase 2 conceptual arc from 02-UI-SPEC.md:
Overview, Umbrella framework, Abstraction, Information, Reliability, Coordination, Temporal, Economics, Model Routing, Human Interaction, Quality, Security, Governance, Accretion.

Required labels from 02-UI-SPEC.md:
Book spine, Open umbrella framework, Open {Pillar} source detail, Canonical PDF, Canonical .tex source, Bibliography source, Source trail, Previous: {title}, Next: {title}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the conceptual-arc book spine with tests</name>
  <files>site/src/data/book-spine.ts, site/src/data/book-spine.test.ts</files>
  <read_first>
    - site/src/data/book-spine.ts if it already exists
    - site/src/data/book-spine.test.ts if it already exists
    - site/src/data/corpus.ts
    - site/src/data/corpus.schema.ts
    - site/src/data/corpus.test.ts
    - .planning/phases/02-book-shell-and-formal-reading-interface/02-UI-SPEC.md
    - .planning/phases/02-book-shell-and-formal-reading-interface/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: bookSpine ids equal overview, umbrella, abstraction, information, reliability, coordination, temporal, economics, model-routing, human-interaction, quality, security, governance, accretion.
    - Test 2: pillarArc has exactly twelve ids and excludes umbrella and overview.
    - Test 3: every corpus-backed spine item uses href `/corpus/{slug}/` and every id exists in corpusEntries.
    - Test 4: getPreviousNext('umbrella') returns previous Overview and next Abstraction; getPreviousNext('overview') has no previous; getPreviousNext('accretion') has no next.
    - Test 5: bookSidebar exposes a group labelled `Book spine` and uses the same href/title data as bookSpine.
  </behavior>
  <action>Create `book-spine.test.ts` first using the Vitest import/order assertion analog from `site/src/data/corpus.test.ts` mapped in `02-PATTERNS.md`, then implement `book-spine.ts`. Export `pillarArc`, `bookSpine`, `bookSidebar`, and `getPreviousNext(currentId: string)`. Use `corpusEntries` as the only source for corpus titles/slugs. The conceptual-arc order is locked per D-05 and D-06: overview, umbrella, abstraction, information, reliability, coordination, temporal, economics, model-routing, human-interaction, quality, security, governance, accretion. Do not use inventory order or alphabetical order. Use href `/` for overview and `/corpus/{entry.slug}/` for corpus entries.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/data/book-spine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/data/book-spine.ts` exports `pillarArc`, `bookSpine`, `bookSidebar`, and `getPreviousNext`.
    - `bookSpine.map((item) => item.id)` matches the exact conceptual-arc order listed in this task.
    - `bookSidebar` contains the exact label `Book spine`.
    - No canonicalTex, canonicalPdf, or bibliography path literals are duplicated in `book-spine.ts`.
  </acceptance_criteria>
  <done>Shared spine data and helper tests pass, proving the order and previous/next contract from one module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate source-detail pages and navigation from the shared spine</name>
  <files>site/astro.config.mjs, site/src/pages/corpus/[slug].astro, site/src/components/SourceLinkPanel.astro, site/src/components/BookFooterNav.astro, site/src/styles/atlas.css</files>
  <read_first>
    - site/astro.config.mjs
    - site/src/pages/corpus/[slug].astro if it already exists
    - site/src/components/SourceLinkPanel.astro if it already exists
    - site/src/components/BookFooterNav.astro if it already exists
    - site/src/styles/atlas.css if it already exists
    - site/src/data/book-spine.ts
    - site/src/data/corpus.ts
    - site/src/data/corpus.schema.ts
    - site/src/pages/inventory.astro
    - site/src/styles/phase-1.css
    - .planning/phases/02-book-shell-and-formal-reading-interface/02-PATTERNS.md
    - .planning/phases/02-book-shell-and-formal-reading-interface/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Build emits `/corpus/umbrella/` and `/corpus/{slug}/` for all twelve pillars.
    - Source panels show summary, source status label, material-kind treatment for exactly `canonical`, `supporting`, `synthesis/review`, and `archived/provenance`, `Canonical .tex source`, `Canonical PDF`, and optional `Bibliography source` labels from corpusEntries.
    - Footer links use exact labels `Previous: {title}` and `Next: {title}` from getPreviousNext.
    - Sidebar reads `bookSidebar` and shows overview, umbrella, and all twelve pillars under `Book spine`.
    - Generated `/corpus/umbrella/` and `/corpus/accretion/` pages include a stable `Book spine` navigation region with current-page state.
  </behavior>
  <action>Update `astro.config.mjs` using the Starlight customCss/sidebar analog from `site/astro.config.mjs` in `02-PATTERNS.md` to import `bookSidebar` from `./src/data/book-spine` and set Starlight `customCss` to include both `katex/dist/katex.min.css` and `./src/styles/atlas.css`. Keep `output: 'static'`, `remarkMath`, and `rehypeKatex` unchanged. Create `SourceLinkPanel.astro` using the source metadata definition-list analog from `site/src/pages/inventory.astro` in `02-PATTERNS.md`; it accepts a CorpusEntry-shaped `entry`, renders heading `Source trail`, visible status text from `sourceStatusLabels`, labelled links `Canonical .tex source`, `Canonical PDF`, and optional `Bibliography source`, plus visible project-root-relative code paths. Add a visible `Material kind` treatment that uses exactly `canonical`, `supporting`, `synthesis/review`, and `archived/provenance`: canonical for active `science/paper/` and `pillars/*/paper/` sources; supporting for pillar-local `notes/` or `research/`; synthesis/review for `science/synthesis/`, `science/reviews/`, or `pillars/*/reviews/`; archived/provenance for archive-backed provenance material only. The source panel must show the current entry as canonical and also include the four-label material-kind legend/treatment so BOOK-06 is explicit, not inferred from source-status colors. Create `BookFooterNav.astro` using the data-driven rendering and focus-style analogs from `site/src/pages/inventory.astro` and `site/src/styles/phase-1.css` in `02-PATTERNS.md`; it accepts `currentId`, calls `getPreviousNext`, and renders secondary footer links labelled `Previous: {title}` and `Next: {title}`. Create `pages/corpus/[slug].astro` with `getStaticPaths()` over `corpusEntries`, `data-pagefind-body`, summary text, `SourceLinkPanel`, and `BookFooterNav`; keep these pages source-detail only per D-03 and D-07, not curated Phase 3 chapters. Because plain Astro dynamic pages do not automatically receive Starlight docs chrome, render a shared `Book spine` navigation region directly from `bookSpine` on this page, preserve current-page state with `aria-current="page"` plus visible text/marker, and verify both `/corpus/umbrella/` and `/corpus/accretion/` show the stable Book spine. Do not create a second hard-coded nav order; the generated page sidebar must import and map `bookSpine`. Create `atlas.css` with UI-SPEC tokens: Dominant `#F7F3EA`, Secondary `#E8DDC8`, Accent `#7A3E1D`, Destructive `#B42318`, text `#1F2933`, muted `#475569`, success `#166534`, sizes 14/16/20/28px, sans stack for prose/interface, monospace stack for paths, 2px Accent `:focus-visible` outline with 2px offset, 44px minimum interactive target, responsive one-column reflow under 720px, source-status variants for all four source-status labels, and material-kind variants for exactly `canonical`, `supporting`, `synthesis/review`, and `archived/provenance`. Implement D-08 by keeping footer nav subtle and sidebar primary.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/data/book-spine.test.ts && bun run check && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `site/astro.config.mjs` imports `bookSidebar` and contains `./src/styles/atlas.css` in `customCss`.
    - `site/src/pages/corpus/[slug].astro` exports `getStaticPaths` and maps over `corpusEntries`.
    - Generated build output includes `dist/corpus/umbrella/index.html` and `dist/corpus/accretion/index.html` after `bun run build`.
    - Source-detail HTML contains the strings `Source trail`, `Material kind`, `canonical`, `supporting`, `synthesis/review`, `archived/provenance`, `Canonical .tex source`, `Canonical PDF`, and `Previous:` or `Next:` where applicable.
    - `site/src/styles/atlas.css` contains `#F7F3EA`, `#E8DDC8`, `#7A3E1D`, `#B42318`, `:focus-visible`, `min-height: 44px`, and selectors/classes for material-kind treatment.
  </acceptance_criteria>
  <done>Readers can use the stable book spine and open every generated source-detail page with source links, material-kind treatment, source-status treatment, current-page Book spine navigation, and previous/next navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpusEntries→static pages | Validated local corpus metadata becomes public source-detail links and labels. |
| Starlight config→navigation UI | Build-time sidebar config controls reader navigation. |
| CSS→accessibility surface | Visual polish can accidentally deny access via poor contrast, hidden focus, or broken reflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | SourceLinkPanel.astro | mitigate | Render canonical source/PDF/bib links from validated `corpusEntries` only; do not duplicate page-local path literals. |
| T-02-01-02 | Tampering | book-spine.ts | mitigate | Vitest locks the exact conceptual-arc order and all hrefs so sidebar/footer/source pages cannot silently drift. |
| T-02-01-03 | Repudiation | corpus/[slug].astro | mitigate | Display source status label text, exact material-kind labels, and project-root-relative paths so source provenance is visible in the UI. |
| T-02-01-04 | Denial of Service | atlas.css focus/reflow | mitigate | Preserve 2px visible focus outlines, 44px targets, and single-column reflow under 720px. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task is planned; use existing Astro/Starlight/Bun dependencies only. |
</threat_model>

<verification>
Run `cd /home/prannayag/harness_eng/site && bun run test -- src/data/book-spine.test.ts && bun run check && bun run build`.
</verification>

<success_criteria>
- `bookSpine` defines one conceptual-arc order for overview, umbrella, and all twelve pillars.
- Starlight sidebar uses `bookSidebar` under the `Book spine` label.
- `/corpus/umbrella/` and all twelve `/corpus/{pillar}/` pages are statically generated.
- Every source-detail page has source status, material-kind treatment for canonical/supporting/synthesis-review/archived-provenance categories, canonical source link, canonical PDF link, optional bibliography link, current-page Book spine navigation, and subtle previous/next footer navigation.
</success_criteria>

<output>
Create `.planning/phases/02-book-shell-and-formal-reading-interface/02-01-SUMMARY.md` when done.
</output>
