---
phase: 01-site-foundation-and-provenance-contract
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - site/src/scripts/validate-corpus.ts
  - site/src/scripts/validate-corpus.test.ts
  - site/src/content/docs/provenance-contract.mdx
requirements: [FOUND-04, FOUND-05, FOUND-06, FOUND-07]
autonomous: true
must_haves:
  truths:
    - "Validation fails when a required corpus id is missing."
    - "Validation fails when required canonical source paths are missing."
    - "Validation fails when canonical source fields point into archive paths unless the entry is provenance-only."
    - "Validation diagnostics name the entry id, field, path, reason, and next fix step."
    - "Contributor-facing docs explain the executable provenance contract and failure examples."
  artifacts:
    - path: "site/src/scripts/validate-corpus.ts"
      provides: "Executable provenance validator"
      contains: "Archive paths cannot be canonical sources"
    - path: "site/src/scripts/validate-corpus.test.ts"
      provides: "Negative validation fixtures"
      contains: "missing required corpus entry"
    - path: "site/src/content/docs/provenance-contract.mdx"
      provides: "Contributor-facing provenance contract docs"
      contains: "Entry security: canonicalTex points to pillars/security/archive/security_architecture.tex"
  key_links:
    - from: "site/package.json"
      to: "site/src/scripts/validate-corpus.ts"
      via: "validate script"
      pattern: "bun run src/scripts/validate-corpus.ts"
    - from: "site/src/scripts/validate-corpus.ts"
      to: "site/src/data/corpus.ts"
      via: "imports corpusEntries and validates paths against repository root"
      pattern: "from '../data/corpus'"
---

<objective>
Implement the executable provenance gate and contributor docs that make invalid canonical paths impossible to build past.

Purpose: Implement D-04, D-13, D-15, D-16 and FOUND-05/FOUND-06 by failing fast on missing, traversal, absolute, and archive-backed canonical references.
Output: Validator script, validator tests, and provenance contract docs.
</objective>

<phase_goal>
**As a** builder of the Harness Architecture Book Wiki, **I want to** run validation that rejects missing or archive-backed canonical sources, **so that** the static site cannot publish untraceable corpus provenance.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-CONTEXT.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-RESEARCH.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-UI-SPEC.md
@site/src/data/corpus.schema.ts
@site/src/data/corpus.ts
@site/package.json
@science/README.md
@pillars/README.md
@docs/migration/final-papers-rearchitecture-manifest.md

<interfaces>
Create these exports in `site/src/scripts/validate-corpus.ts` for tests and CLI reuse:
```typescript
export interface ValidationError {
  entryId: string;
  field: string;
  path: string;
  reason: string;
  nextStep: string;
}
export interface ValidationResult {
  ok: boolean;
  errors: ValidationError[];
}
export function validateCorpus(entries?: CorpusEntry[], options?: { repoRoot?: string }): ValidationResult;
export function formatValidationError(error: ValidationError): string;
```
The formatted error pattern must be exactly: `Entry {id}: {field} points to {path}. {reason}. {next step}.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test negative provenance validation cases</name>
  <files>site/src/scripts/validate-corpus.test.ts, site/src/scripts/validate-corpus.ts</files>
  <read_first>site/src/data/corpus.schema.ts, site/src/data/corpus.ts, .planning/phases/01-site-foundation-and-provenance-contract/01-UI-SPEC.md, .planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md</read_first>
  <behavior>
    - Test 1: removing the `security` entry returns `ok: false` with reason containing `Missing required corpus entry`.
    - Test 2: changing `security.canonicalTex` to `pillars/security/archive/security_architecture.tex` with `sourceStatus: 'canonical'` returns formatted error exactly `Entry security: canonicalTex points to pillars/security/archive/security_architecture.tex. Archive paths cannot be canonical sources. Move the reference to pillars/security/paper/ or mark the entry as provenance-only.`
    - Test 3: changing `umbrella.canonicalTex` to `science/paper/does-not-exist.tex` returns `ok: false` and a formatted error naming `umbrella`, `canonicalTex`, and the missing path.
    - Test 4: absolute paths and `..` path segments return `ok: false` with reasons `Path must be project-root-relative` and `Path cannot contain parent traversal`.
  </behavior>
  <action>Apply PATTERNS.md analog for `site/src/scripts/validate-corpus.ts`: mirror `latex-document-skill/scripts/validate_latex.py` by collecting structured validation errors, testing missing-file/path failures, and returning non-zero from the CLI on errors. Create `site/src/scripts/validate-corpus.test.ts` first with Vitest cases for missing id, archive-as-canonical, missing filesystem path, absolute path, and parent traversal. Add a temporary skeleton `site/src/scripts/validate-corpus.ts` if needed so tests import, confirm RED, then proceed in Task 2. Use fixture entries by cloning `corpusEntries`; do not mutate the exported inventory. The tests must assert the exact UI-SPEC diagnostic sentence for the archive case.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/scripts/validate-corpus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Missing required corpus entry" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.test.ts` succeeds.
    - `grep -q "Entry security: canonicalTex points to pillars/security/archive/security_architecture.tex. Archive paths cannot be canonical sources. Move the reference to pillars/security/paper/ or mark the entry as provenance-only." /home/prannayag/harness_eng/site/src/scripts/validate-corpus.test.ts` succeeds.
    - `grep -q "Path must be project-root-relative" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.test.ts` succeeds.
    - `grep -q "Path cannot contain parent traversal" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.test.ts` succeeds.
  </acceptance_criteria>
  <done>Negative validation behavior is specified in automated tests before implementation is completed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-fast corpus validator CLI</name>
  <files>site/src/scripts/validate-corpus.ts, site/src/scripts/validate-corpus.test.ts</files>
  <read_first>site/src/scripts/validate-corpus.test.ts, site/src/data/corpus.schema.ts, site/src/data/corpus.ts, science/README.md, pillars/README.md</read_first>
  <behavior>
    - Test 1: real `corpusEntries` validates with `ok: true`.
    - Test 2: CLI prints `OK: 13 corpus entries validated, 0 errors found.` and exits 0 on the real inventory.
    - Test 3: invalid fixtures collect all validation errors rather than throwing after the first one.
  </behavior>
  <action>Implement `site/src/scripts/validate-corpus.ts` using the PATTERNS.md `latex-document-skill/scripts/validate_latex.py` analog: structured error objects, plain and JSON output, all-errors collection, filesystem existence checks, and explicit exit code behavior. Import `existsSync` from `node:fs`, `resolve`, `isAbsolute`, and path helpers from `node:path`, `fileURLToPath` from `node:url`, `corpusEntries` from `../data/corpus`, and `expectedCorpusIds`, `CorpusEntry`, `parseCorpusEntries` from `../data/corpus.schema`. Validation rules: parse entries through Zod; require exactly all `expectedCorpusIds`; reject duplicate ids; for fields `canonicalTex`, `canonicalPdf`, and optional `bibliography`, reject absolute paths with reason `Path must be project-root-relative`; reject any segment `..` with reason `Path cannot contain parent traversal`; reject paths matching `/(^|\/)archive(\/|$)/` unless `entry.sourceStatus === 'provenance-only'` with reason `Archive paths cannot be canonical sources` and next step `Move the reference to pillars/security/paper/ or mark the entry as provenance-only` for the security archive test, otherwise `Move the reference to the owning paper/ directory or mark the entry as provenance-only`; resolve paths against the repository root one level above `site/` and fail missing paths with reason `Required canonical source path does not exist`; require `.tex` suffix for `canonicalTex`; require `.pdf` suffix for `canonicalPdf`; require `.bib` suffix for bibliography when present. Implement CLI behavior: plain output by default, optional `--json`, print diagnostics to stderr on errors, print OK to stdout on success, and `process.exit(1)` if errors exist. Do not write to `science/`, `pillars/`, `archive/`, or repository root.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/scripts/validate-corpus.test.ts && bun run validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function validateCorpus" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts` succeeds.
    - `grep -q "export function formatValidationError" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts` succeeds.
    - `grep -q "Archive paths cannot be canonical sources" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts` succeeds.
    - `grep -q "Required canonical source path does not exist" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts` succeeds.
    - `grep -q "Path cannot contain parent traversal" /home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts` succeeds.
    - `grep -q "OK: 13 corpus entries validated, 0 errors found." /home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts` succeeds.
  </acceptance_criteria>
  <done>`bun run validate` hard-fails invalid provenance and succeeds on the real thirteen-entry inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Document executable provenance contract</name>
  <files>site/src/content/docs/provenance-contract.mdx</files>
  <read_first>.planning/phases/01-site-foundation-and-provenance-contract/01-UI-SPEC.md, site/src/scripts/validate-corpus.ts, science/README.md, pillars/README.md, docs/migration/final-papers-rearchitecture-manifest.md</read_first>
  <action>Apply PATTERNS.md analogs for `site/src/content/docs/provenance-contract.mdx`: use `docs/migration/final-papers-rearchitecture-manifest.md` for provenance table/traceability tone, `assets/latex/README.md` for explicit local-only exceptions, and `science/README.md` for archive-as-non-canonical language. Create `site/src/content/docs/provenance-contract.mdx` with frontmatter `title: Provenance Contract`. Document that `bun run validate` is the executable gate and `bun run build` runs validation before static HTML and indexes. Include required inventory fields exactly: `id`, `kind`, `title`, `slug`, `summary`, `canonicalTex`, `canonicalPdf`, `bibliography`, `sourceStatus`. State canonical source paths must point to `science/paper/` or `pillars/*/paper/`, must be project-root-relative, must not contain `..`, and archive paths cannot be canonical sources unless the entry is `provenance-only` per D-16. Include pass example text `OK: 13 corpus entries validated, 0 errors found.` Include missing-path failure example using `Entry umbrella: canonicalTex points to science/paper/does-not-exist.tex. Required canonical source path does not exist. Restore the file or update the inventory to the canonical paper path.` Include archive failure example exactly from UI-SPEC: `Entry security: canonicalTex points to pillars/security/archive/security_architecture.tex. Archive paths cannot be canonical sources. Move the reference to pillars/security/paper/ or mark the entry as provenance-only.` Do not document deployment, accounts, CMS, hosted search, formal object anchors, or graph metadata as Phase 1 requirements.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run build:astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "bun run validate" /home/prannayag/harness_eng/site/src/content/docs/provenance-contract.mdx` succeeds.
    - `grep -q "OK: 13 corpus entries validated, 0 errors found." /home/prannayag/harness_eng/site/src/content/docs/provenance-contract.mdx` succeeds.
    - `grep -q "Entry security: canonicalTex points to pillars/security/archive/security_architecture.tex. Archive paths cannot be canonical sources. Move the reference to pillars/security/paper/ or mark the entry as provenance-only." /home/prannayag/harness_eng/site/src/content/docs/provenance-contract.mdx` succeeds.
    - `grep -q "science/paper/" /home/prannayag/harness_eng/site/src/content/docs/provenance-contract.mdx` succeeds.
    - `grep -q "pillars/\*/paper/" /home/prannayag/harness_eng/site/src/content/docs/provenance-contract.mdx` succeeds.
    - `grep -q "sourceStatus" /home/prannayag/harness_eng/site/src/content/docs/provenance-contract.mdx` succeeds.
  </acceptance_criteria>
  <done>Contributor-facing docs describe the exact executable contract and validation failure diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory path strings -> filesystem | Untrusted-by-default strings are resolved against the repository filesystem during validation. |
| validator diagnostics -> builder decisions | Error text guides contributors to canonical paths or provenance-only marking. |
| build script -> corpus directories | Build validation reads corpus paths and must not mutate canonical sources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | T | `validateCorpus` path resolution | mitigate | Reject absolute paths and `..` segments before filesystem resolution. |
| T-01-10 | T | archive path handling | mitigate | Reject `/(^|/)archive(/|$)/` for canonical fields unless `sourceStatus === 'provenance-only'`. |
| T-01-11 | R | validation output | mitigate | Format every error as `Entry {id}: {field} points to {path}. {reason}. {next step}.` so failures identify responsible data. |
| T-01-12 | D | `bun run build` chain | mitigate | `site/package.json` already makes build depend on `bun run validate`; this plan implements the target command. |
| T-01-13 | I | filesystem diagnostics | mitigate | Diagnostics print project-relative inventory paths only, not absolute resolved host paths. |
</threat_model>

<verification>
Run from repository root:
```bash
cd /home/prannayag/harness_eng/site && bun run test -- src/scripts/validate-corpus.test.ts && bun run validate && bun run build:astro
```
</verification>

<success_criteria>
- `bun run validate` succeeds on the real inventory.
- Tests prove missing ids, missing source paths, archive-as-canonical paths, absolute paths, and parent traversal fail.
- Provenance docs include exact pass/fail examples and required field list.
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-foundation-and-provenance-contract/01-03-SUMMARY.md`.
</output>
