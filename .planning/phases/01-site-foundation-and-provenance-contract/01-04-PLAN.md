---
phase: 01-site-foundation-and-provenance-contract
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - site/src/scripts/generate-local-indexes.ts
  - site/src/scripts/generate-local-indexes.test.ts
  - site/src/pages/inventory.astro
  - site/src/styles/phase-1.css
  - site/src/content/docs/index.mdx
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-07]
autonomous: true
must_haves:
  truths:
    - "Builder can run one documented command that validates, builds static HTML, and emits local indexes."
    - "Builder can open a static inventory page rendered from the real thirteen-entry inventory."
    - "The inventory page exposes title, kind, slug, source status, canonical .tex path, PDF path, and bibliography path when present."
    - "Generated local indexes live under site/dist and do not write into science/ or pillars/."
    - "D-07: Phase 1 UI uses the approved early theme foundation status labels, copy, colors, focus behavior, and responsive source path wrapping without building the full custom reader-facing shell."
  artifacts:
    - path: "site/src/scripts/generate-local-indexes.ts"
      provides: "Deterministic corpus JSON index writer"
      contains: "corpus-index.json"
    - path: "site/src/pages/inventory.astro"
      provides: "Static inventory page"
      contains: "sourceStatusLabels"
    - path: "site/src/styles/phase-1.css"
      provides: "UI-SPEC foundation tokens"
      contains: "#F7F3EA"
    - path: "site/src/content/docs/index.mdx"
      provides: "Documented one-command static build"
      contains: "bun run build"
  key_links:
    - from: "site/package.json"
      to: "site/src/scripts/generate-local-indexes.ts"
      via: "index script writes dist/corpus-index.json before Pagefind"
      pattern: "generate-local-indexes"
    - from: "site/src/pages/inventory.astro"
      to: "site/src/data/corpus.ts"
      via: "imports corpusEntries for static rendering"
      pattern: "corpusEntries"
    - from: "site/src/styles/phase-1.css"
      to: "site/src/pages/inventory.astro"
      via: "class names and global stylesheet import"
      pattern: "phase-1.css"
---

<objective>
Complete the walking skeleton vertical slice: generated local corpus index, static inventory page, Phase 1 UI tokens, and final one-command build.

Purpose: Prove FOUND-01/FOUND-02/FOUND-03/FOUND-07 end to end by rendering the real inventory into static HTML and local indexes after validation.
Output: Index generator, inventory page, UI foundation CSS, and updated landing command documentation.
</objective>

<phase_goal>
**As a** builder of the Harness Architecture Book Wiki, **I want to** run one command and inspect static inventory output, **so that** the site foundation proves the corpus contract end-to-end without a server or hosted search.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-CONTEXT.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-RESEARCH.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-UI-SPEC.md
@site/package.json
@site/src/data/corpus.schema.ts
@site/src/data/corpus.ts
@site/src/scripts/validate-corpus.ts
@site/src/content/docs/index.mdx
@README.md

<interfaces>
The generated `site/dist/corpus-index.json` must have this top-level shape:
```json
{
  "generatedBy": "site/src/scripts/generate-local-indexes.ts",
  "entryCount": 13,
  "entries": [
    {
      "id": "umbrella",
      "kind": "umbrella",
      "title": "A Formal Framework for AI Coding Agent Harness Architecture",
      "slug": "umbrella",
      "summary": "...",
      "sourceStatus": "canonical",
      "canonicalTex": "science/paper/science.tex",
      "canonicalPdf": "science/paper/science.pdf",
      "bibliography": "science/paper/science.bib"
    }
  ]
}
```
The inventory page must import `corpusEntries` and `sourceStatusLabels` directly from concrete module paths, not from a barrel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate deterministic local corpus index</name>
  <files>site/src/scripts/generate-local-indexes.ts, site/src/scripts/generate-local-indexes.test.ts</files>
  <read_first>site/src/data/corpus.ts, site/src/data/corpus.schema.ts, site/src/scripts/validate-corpus.ts, .planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md, site/package.json</read_first>
  <behavior>
    - Test 1: generator writes `corpus-index.json` under a provided temporary output directory with `entryCount: 13`.
    - Test 2: generated entries preserve `umbrella` first and include `security` and `accretion`.
    - Test 3: generator refuses output paths outside the provided site output directory when called with a path containing `..`.
  </behavior>
  <action>Apply PATTERNS.md analog for `site/src/scripts/generate-local-indexes.ts`: follow `latex-document-skill/scripts/generate_chart.py` for deterministic input transform, parent output directory creation, stderr success/failure messages, and non-zero write failure exits. Create `site/src/scripts/generate-local-indexes.test.ts` first, then implement `site/src/scripts/generate-local-indexes.ts`. Export `buildCorpusIndex(entries = corpusEntries)` and `writeCorpusIndex(options?: { outputDir?: string })`. The CLI default output directory must be `dist`, resolved inside `site/`, and must write `dist/corpus-index.json`. The JSON must be deterministic: two-space indentation, entries in `corpusEntries` order, no timestamps. Include top-level `generatedBy: 'site/src/scripts/generate-local-indexes.ts'`, `entryCount`, and `entries`. Create parent directories with `mkdirSync(..., { recursive: true })`. Print `Successfully created: dist/corpus-index.json` to stderr on success and exit non-zero on write failures. Do not write to `science/`, `pillars/`, `archive/`, or repository root. The test for `..` must assert the error message contains `Output directory must stay inside site/`.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/scripts/generate-local-indexes.test.ts && bun run index</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "corpus-index.json" /home/prannayag/harness_eng/site/src/scripts/generate-local-indexes.ts` succeeds.
    - `grep -q "generatedBy: 'site/src/scripts/generate-local-indexes.ts'" /home/prannayag/harness_eng/site/src/scripts/generate-local-indexes.ts` succeeds.
    - `grep -q "Successfully created: dist/corpus-index.json" /home/prannayag/harness_eng/site/src/scripts/generate-local-indexes.ts` succeeds.
    - `grep -q "Output directory must stay inside site/" /home/prannayag/harness_eng/site/src/scripts/generate-local-indexes.test.ts` succeeds.
    - `test -f /home/prannayag/harness_eng/site/dist/corpus-index.json` succeeds after `bun run index`.
  </acceptance_criteria>
  <done>`bun run index` writes deterministic local index artifacts under `site/dist` and never writes into canonical corpus directories.</done>
</task>

<task type="auto">
  <name>Task 2: Render static inventory page with UI-SPEC tokens</name>
  <files>site/src/pages/inventory.astro, site/src/styles/phase-1.css, site/src/content/docs/index.mdx</files>
  <read_first>.planning/phases/01-site-foundation-and-provenance-contract/01-UI-SPEC.md, .planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md, site/src/data/corpus.ts, site/src/data/corpus.schema.ts, site/src/content/docs/index.mdx</read_first>
  <action>Apply PATTERNS.md analogs: for `site/src/pages/inventory.astro`, use `README.md`, `science/README.md`, and `pillars/README.md` inventory/table fields as the static render model; for `site/src/styles/phase-1.css`, follow `.obsidian/plugins/harness-canvas-research/styles.css` component class/focus-visible pattern but replace Obsidian variables with UI-SPEC tokens. Create `site/src/styles/phase-1.css` with CSS custom properties exactly: `--phase-color-dominant: #F7F3EA;`, `--phase-color-secondary: #E8DDC8;`, `--phase-color-accent: #7A3E1D;`, `--phase-color-destructive: #B42318;`, spacing tokens `--space-xs: 4px;`, `--space-sm: 8px;`, `--space-md: 16px;`, `--space-lg: 24px;`, `--space-xl: 32px;`, `--space-2xl: 48px;`, `--space-3xl: 64px;`, typography sizes `16px`, `14px`, `20px`, `28px`, and focus rule `outline: 2px solid var(--phase-color-accent); outline-offset: 2px;`. Create `site/src/pages/inventory.astro` importing `../styles/phase-1.css`, `corpusEntries`, and `sourceStatusLabels`. Render one `h1`, a summary stating `13 corpus entries`, and exactly one card/row per entry. Each entry must expose title, kind, slug, status label text, canonical `.tex` path, PDF path, and bibliography path when present. Source paths must use monospace styling and `overflow-wrap: anywhere`; below `720px`, metadata must stack vertically. Status labels must be text from `Canonical`, `Missing source`, `Archive blocked`, and `Provenance-only`; do not communicate status by color alone. Update `site/src/content/docs/index.mdx` only to ensure it links to `/inventory/` with exact text `Inspect Corpus Inventory` and states `bun run build` produces static HTML assets, `dist/corpus-index.json`, and Pagefind local index files.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "#F7F3EA" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "#E8DDC8" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "#7A3E1D" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "#B42318" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "outline: 2px solid var(--phase-color-accent)" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "overflow-wrap: anywhere" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "@media (max-width: 720px)" /home/prannayag/harness_eng/site/src/styles/phase-1.css` succeeds.
    - `grep -q "corpusEntries" /home/prannayag/harness_eng/site/src/pages/inventory.astro` succeeds.
    - `grep -q "sourceStatusLabels" /home/prannayag/harness_eng/site/src/pages/inventory.astro` succeeds.
    - `grep -q "13 corpus entries" /home/prannayag/harness_eng/site/src/pages/inventory.astro` succeeds.
    - `grep -q "Inspect Corpus Inventory" /home/prannayag/harness_eng/site/src/content/docs/index.mdx` succeeds.
    - `test -f /home/prannayag/harness_eng/site/dist/index.html` succeeds after `bun run build`.
    - `test -f /home/prannayag/harness_eng/site/dist/inventory/index.html` succeeds after `bun run build`.
    - `test -f /home/prannayag/harness_eng/site/dist/corpus-index.json` succeeds after `bun run build`.
  </acceptance_criteria>
  <done>`bun run build` validates provenance, builds static HTML, renders the inventory page from real data, and emits local index artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated inventory -> generated JSON | Build script writes public static index data from inventory fields. |
| validated inventory -> static HTML | Inventory page renders source path strings into HTML. |
| build output -> local search index | Pagefind indexes static HTML into client-readable local assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | T | `generate-local-indexes.ts` output path | mitigate | Resolve output inside `site/` and reject `..` with `Output directory must stay inside site/`. |
| T-01-15 | I | `inventory.astro` source paths | mitigate | Render only repository-relative paths already validated by Plan 03; no absolute resolved paths are displayed. |
| T-01-16 | D | `bun run build` | mitigate | Build chains `validate`, `build:astro`, and `index`; invalid provenance blocks HTML/index output. |
| T-01-17 | S | status badges | mitigate | Render status labels from `sourceStatusLabels`, preventing color-only or misleading status presentation. |
| T-01-18 | I | local search/index artifacts | accept | Phase 1 publishes only public corpus metadata for a static research site; no accounts, secrets, or private data are involved. |
</threat_model>

<verification>
Run from repository root:
```bash
cd /home/prannayag/harness_eng/site && bun run test && bun run build
```
Then confirm:
```bash
test -f /home/prannayag/harness_eng/site/dist/index.html && test -f /home/prannayag/harness_eng/site/dist/inventory/index.html && test -f /home/prannayag/harness_eng/site/dist/corpus-index.json
```
</verification>

<success_criteria>
- One documented command, `cd site && bun run build`, validates, builds static HTML, and emits local indexes.
- Inventory page renders exactly the typed corpus inventory fields required by UI-SPEC.
- CSS implements Phase 1 colors, typography, spacing, focus, and responsive source path behavior.
- Generated output remains under `site/dist` and no canonical corpus files are mutated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-foundation-and-provenance-contract/01-04-SUMMARY.md`.
</output>
