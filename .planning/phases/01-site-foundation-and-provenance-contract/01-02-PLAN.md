---
phase: 01-site-foundation-and-provenance-contract
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - site/src/data/corpus.schema.ts
  - site/src/data/corpus.ts
  - site/src/data/corpus.test.ts
requirements: [FOUND-03, FOUND-04]
autonomous: true
must_haves:
  truths:
    - "Builder can inspect a typed inventory covering exactly the umbrella framework and all twelve pillars."
    - "Every inventory entry carries required canonical source metadata."
    - "Inventory source statuses are finite and include canonical, missing-source, archive-blocked, and provenance-only."
  artifacts:
    - path: "site/src/data/corpus.schema.ts"
      provides: "Zod schema, TypeScript types, expected ids, status labels"
      contains: "expectedCorpusIds"
    - path: "site/src/data/corpus.ts"
      provides: "Explicit thirteen-entry inventory"
      contains: "canonicalTex"
    - path: "site/src/data/corpus.test.ts"
      provides: "Unit tests for schema and coverage"
      contains: "expect(corpusEntries).toHaveLength(13)"
  key_links:
    - from: "site/src/data/corpus.ts"
      to: "site/src/data/corpus.schema.ts"
      via: "corpusEntries satisfies CorpusEntry[] and parseCorpusEntries"
      pattern: "parseCorpusEntries"
    - from: "site/src/data/corpus.ts"
      to: "science/paper/ and pillars/*/paper/"
      via: "project-root-relative canonicalTex/canonicalPdf/bibliography strings"
      pattern: "canonicalTex: 'science/paper/science.tex'"
---

<objective>
Create the explicit typed corpus inventory and schema contract that later validation, pages, and local indexes consume.

Purpose: Implement D-09 through D-12 and FOUND-03/FOUND-04 by making the umbrella plus twelve-pillar source contract concrete, typed, and testable.
Output: `corpus.schema.ts`, `corpus.ts`, and inventory unit tests.
</objective>

<phase_goal>
**As a** builder of the Harness Architecture Book Wiki, **I want to** inspect a typed corpus inventory with required provenance metadata, **so that** the static site foundation knows the corpus boundaries before content pages scale.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-CONTEXT.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-RESEARCH.md
@.planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md
@README.md
@science/README.md
@pillars/README.md
@docs/migration/final-papers-rearchitecture-manifest.md

<interfaces>
Create and export these contracts from `site/src/data/corpus.schema.ts`:
```typescript
export const sourceStatusSchema = z.enum(['canonical', 'missing-source', 'archive-blocked', 'provenance-only']);
export const sourceStatusLabels = {
  canonical: 'Canonical',
  'missing-source': 'Missing source',
  'archive-blocked': 'Archive blocked',
  'provenance-only': 'Provenance-only',
} as const;
export const corpusKindSchema = z.enum(['umbrella', 'pillar']);
export const expectedCorpusIds = [
  'umbrella',
  'abstraction',
  'information',
  'reliability',
  'coordination',
  'temporal',
  'quality',
  'governance',
  'economics',
  'human-interaction',
  'model-routing',
  'security',
  'accretion',
] as const;
```
Each `CorpusEntry` must include `id`, `kind`, `title`, `slug`, `summary`, `canonicalTex`, `canonicalPdf`, optional `bibliography`, and `sourceStatus`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define corpus schema and coverage tests</name>
  <files>site/src/data/corpus.schema.ts, site/src/data/corpus.test.ts</files>
  <read_first>.planning/phases/01-site-foundation-and-provenance-contract/01-CONTEXT.md, .planning/phases/01-site-foundation-and-provenance-contract/01-RESEARCH.md, .planning/phases/01-site-foundation-and-provenance-contract/01-PATTERNS.md, site/package.json</read_first>
  <behavior>
    - Test 1: `expectedCorpusIds` has exactly 13 ids and includes `umbrella`, `abstraction`, `information`, `reliability`, `coordination`, `temporal`, `quality`, `governance`, `economics`, `human-interaction`, `model-routing`, `security`, and `accretion` in that order.
    - Test 2: `sourceStatusSchema` accepts `canonical`, `missing-source`, `archive-blocked`, and `provenance-only` and rejects `archived`.
    - Test 3: `corpusEntrySchema` rejects an entry missing `canonicalTex`.
  </behavior>
  <action>Apply PATTERNS.md analog for `site/src/data/corpus.schema.ts`: follow `.obsidian/plugins/harness-canvas-research/src/types.ts` finite union/plain interface style, translated to Zod schemas plus exported TypeScript types. Create `site/src/data/corpus.schema.ts` with Zod imports from `zod`. Export `sourceStatusSchema`, `sourceStatusLabels`, `corpusKindSchema`, `expectedCorpusIds`, `corpusEntrySchema`, `corpusEntriesSchema`, `CorpusEntry`, `SourceStatus`, and `parseCorpusEntries(entries: unknown): CorpusEntry[]`. The schema must require `canonicalTex` and `canonicalPdf` for all entries, allow optional `bibliography`, and require non-empty strings for id/title/slug/summary/source paths. Create `site/src/data/corpus.test.ts` using Vitest. Write the tests first, run them to confirm RED before implementation if the files did not exist, then implement the schema until tests pass. Use finite source statuses exactly from D-16/UI-SPEC; do not add `archived`, `draft`, or `unknown` statuses.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/data/corpus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sourceStatusSchema = z.enum(\['canonical', 'missing-source', 'archive-blocked', 'provenance-only'\])" /home/prannayag/harness_eng/site/src/data/corpus.schema.ts` succeeds.
    - `grep -q "export const expectedCorpusIds" /home/prannayag/harness_eng/site/src/data/corpus.schema.ts` succeeds.
    - `grep -q "'human-interaction'" /home/prannayag/harness_eng/site/src/data/corpus.schema.ts` succeeds.
    - `grep -q "'model-routing'" /home/prannayag/harness_eng/site/src/data/corpus.schema.ts` succeeds.
    - `grep -q "canonicalTex" /home/prannayag/harness_eng/site/src/data/corpus.schema.ts` succeeds.
    - `grep -q "expect(sourceStatusSchema.safeParse('archived').success).toBe(false)" /home/prannayag/harness_eng/site/src/data/corpus.test.ts` succeeds.
  </acceptance_criteria>
  <done>Schema contract and tests prove the finite status set, required metadata fields, and thirteen expected corpus ids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author explicit thirteen-entry inventory</name>
  <files>site/src/data/corpus.ts, site/src/data/corpus.test.ts</files>
  <read_first>README.md, science/README.md, pillars/README.md, docs/migration/final-papers-rearchitecture-manifest.md, site/src/data/corpus.schema.ts, site/src/data/corpus.test.ts</read_first>
  <behavior>
    - Test 1: `corpusEntries` parses through `parseCorpusEntries` and has length 13.
    - Test 2: every `expectedCorpusIds` id appears exactly once in `corpusEntries`.
    - Test 3: every entry has `sourceStatus: 'canonical'` for the initial inventory.
  </behavior>
  <action>Apply PATTERNS.md analogs for `site/src/data/corpus.ts`: use `README.md`, `science/README.md`, and `pillars/README.md` for source ordering, role text, and canonical path seed data. Create `site/src/data/corpus.ts` as explicit typed data per D-10, not filesystem scan output. Export `corpusEntries` by passing the literal array through `parseCorpusEntries`. Include exactly these entries and paths: `umbrella` kind `umbrella`, title `A Formal Framework for AI Coding Agent Harness Architecture`, slug `umbrella`, canonicalTex `science/paper/science.tex`, canonicalPdf `science/paper/science.pdf`, bibliography `science/paper/science.bib`; `abstraction` paths `pillars/abstraction/paper/abstraction_architecture.tex`, `.pdf`, `.bib`; `information` paths `pillars/information/paper/information_architecture.tex`, `.pdf`, `.bib`; `reliability` paths `pillars/reliability/paper/reliability_architecture.tex`, `.pdf`, `.bib`; `coordination` paths `pillars/coordination/paper/coordination_architecture.tex`, `.pdf`, `.bib`; `temporal` paths `pillars/temporal/paper/temporal_architecture.tex`, `.pdf`, `.bib`; `quality` paths `pillars/quality/paper/quality_architecture.tex`, `.pdf`, `.bib`; `governance` paths `pillars/governance/paper/governance_architecture.tex`, `.pdf`, `.bib`; `economics` paths `pillars/economics/paper/economics_architecture.tex`, `.pdf`, `.bib`; `human-interaction` paths `pillars/human-interaction/paper/human_interaction_architecture.tex`, `.pdf`, `.bib`; `model-routing` paths `pillars/model-routing/paper/model_routing_architecture.tex`, `.pdf`, `.bib`; `security` paths `pillars/security/paper/security_architecture.tex`, `.pdf`, `.bib`; `accretion` paths `pillars/accretion/paper/accretion_category.tex`, `.pdf`, `.bib`. Use summaries from `pillars/README.md`/`README.md` exactly enough to preserve the focus text. Set all `sourceStatus` values to `canonical` for the initial inventory. Extend `site/src/data/corpus.test.ts` with inventory tests.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/data/corpus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: '" /home/prannayag/harness_eng/site/src/data/corpus.ts` outputs `13`.
    - `grep -q "id: 'umbrella'" /home/prannayag/harness_eng/site/src/data/corpus.ts` succeeds.
    - `grep -q "canonicalTex: 'science/paper/science.tex'" /home/prannayag/harness_eng/site/src/data/corpus.ts` succeeds.
    - `grep -q "canonicalTex: 'pillars/security/paper/security_architecture.tex'" /home/prannayag/harness_eng/site/src/data/corpus.ts` succeeds.
    - `grep -q "canonicalTex: 'pillars/accretion/paper/accretion_category.tex'" /home/prannayag/harness_eng/site/src/data/corpus.ts` succeeds.
    - `grep -q "expect(corpusEntries).toHaveLength(13)" /home/prannayag/harness_eng/site/src/data/corpus.test.ts` succeeds.
    - `grep -q "sourceStatus: 'canonical'" /home/prannayag/harness_eng/site/src/data/corpus.ts` succeeds.
  </acceptance_criteria>
  <done>`corpusEntries` provides the explicit umbrella plus twelve-pillar inventory with required metadata and passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed inventory -> validator/pages | Inventory data becomes build-time authority for provenance display and validation. |
| repository docs -> inventory literals | Human-readable README/pillar summaries are copied into typed site data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | S | `corpusEntries` | mitigate | Require exactly one of each `expectedCorpusIds` in tests; no duplicate or missing corpus identity can pass. |
| T-01-06 | T | `corpus.schema.ts` | mitigate | Zod schema requires `canonicalTex`, `canonicalPdf`, source status, title, slug, summary, and finite kind/status values. |
| T-01-07 | R | `corpus.ts` | mitigate | Explicit inventory references canonical paths directly; no generated scan can later deny source choices. |
| T-01-08 | I | source path strings | accept | Paths are repository-relative public corpus paths; absolute path and traversal rejection is implemented in Plan 03 validator. |
</threat_model>

<verification>
Run from repository root:
```bash
cd /home/prannayag/harness_eng/site && bun run test -- src/data/corpus.test.ts && bun run check
```
</verification>

<success_criteria>
- `site/src/data/corpus.schema.ts` exports the inventory schema, status labels, and expected ids.
- `site/src/data/corpus.ts` exports exactly thirteen explicit entries with canonical metadata.
- Tests prove expected ids, finite statuses, required canonical metadata, and parsed inventory coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-foundation-and-provenance-contract/01-02-SUMMARY.md`.
</output>
