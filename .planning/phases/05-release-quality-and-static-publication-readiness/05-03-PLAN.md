---
phase: 05-release-quality-and-static-publication-readiness
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - site/src/scripts/validate-math-fixtures.ts
  - site/src/scripts/validate-math-fixtures.test.ts
autonomous: true
requirements: [QUAL-02]
user_setup: []
must_haves:
  truths:
    - "Builder can verify representative math/source snippets from the umbrella paper and multiple pillar papers before publishing."
    - "Math fixture failures identify the source file, expected snippet or anchor, reason, and next step."
    - "Representative fixtures connect canonical .tex support to formal registry IDs and rendered anchors when dist exists."
  artifacts:
    - path: "site/src/scripts/validate-math-fixtures.ts"
      provides: "Representative canonical math/source fixture validator"
      exports: ["validateMathFixtures", "formatMathFixtureError"]
    - path: "site/src/scripts/validate-math-fixtures.test.ts"
      provides: "Tests for passing real fixtures and failing missing snippet/anchor fixtures"
  key_links:
    - from: "site/src/scripts/validate-math-fixtures.ts"
      to: "science/paper/science.tex"
      via: "canonical source snippet fixture"
      pattern: "science/paper/science\.tex"
    - from: "site/src/scripts/validate-math-fixtures.ts"
      to: "pillars/*/paper/*.tex"
      via: "representative pillar source fixtures"
      pattern: "pillars/.*/paper/.*\.tex"
    - from: "site/src/scripts/validate-math-fixtures.ts"
      to: "site/src/data/formal-registry.ts"
      via: "formal object ID lookup"
      pattern: "formalRegistry"
---

<objective>
Create the representative math fixture gate: a static validator that proves selected formal/math claims remain grounded in canonical umbrella and pillar `.tex` sources and connect to rendered formal anchors.

Purpose: Academic release readiness needs evidence that math rendering and curated formal objects have not drifted away from canonical corpus sources.
Output: A focused `validate-math-fixtures.ts` script with tests, CLI output, and diagnostics suitable for the unified release orchestrator.
</objective>

<phase_goal>
**As a** static site builder, **I want to** verify representative canonical math snippets against rendered formal anchors, **so that** publication does not ship broken or unsupported formal research claims.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-CONTEXT.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-RESEARCH.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-PATTERNS.md
@site/src/scripts/validate-formal-registry.ts
@site/src/scripts/validate-formal-registry.test.ts
@site/src/data/formal-registry.ts
@site/src/data/chapters.ts
@site/src/data/corpus.ts
@science/paper/science.tex

<interfaces>
Math fixture validator target:
- `MathFixture` includes `id`, `ownerId`, `sourcePath`, `snippet`, `formalObjectId`, and optional `renderedHref`.
- `validateMathFixtures()` returns `{ ok: boolean, errors: MathFixtureError[], fixtures: MathFixture[] }`.
- `MathFixtureError` shape is `{ entryId, field, path, reason, nextStep }` for release-orchestrator normalization.
- Validator checks repository-relative paths only; absolute paths and parent traversal are invalid.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add math fixture validator tests</name>
  <files>site/src/scripts/validate-math-fixtures.test.ts</files>
  <read_first>
    - site/src/scripts/validate-math-fixtures.test.ts if it already exists
    - site/src/scripts/validate-formal-registry.test.ts
    - site/src/scripts/validate-formal-registry.ts
    - site/src/data/formal-registry.ts
    - site/src/data/chapters.ts
    - site/src/data/corpus.ts
    - science/paper/science.tex
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-RESEARCH.md
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-PATTERNS.md
  </read_first>
  <behavior>
    - Real default fixtures pass against the checked canonical sources and existing formal registry.
    - A fixture with `sourcePath: 'science/paper/science.tex'` and a missing snippet fails with `field: 'snippet'`.
    - A fixture with an unknown `formalObjectId` fails with `field: 'formalObjectId'`.
    - A fixture with `sourcePath: '../science/paper/science.tex'` fails with `Path cannot contain parent traversal`.
  </behavior>
  <action>Create `site/src/scripts/validate-math-fixtures.test.ts` using Vitest. Import not-yet-existing `validateMathFixtures` and `formatMathFixtureError` from `./validate-math-fixtures`. Tests must assert the default fixture set includes at least these owner IDs: `umbrella`, `reliability`, `information`, `coordination`, `economics`, and `security`, per research recommendation for umbrella plus multiple pillars. Use fixture injection to test missing snippets, invalid formal object IDs, and parent traversal without editing canonical `.tex` files.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-math-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test source contains `umbrella`, `reliability`, `information`, `coordination`, `economics`, and `security`.
    - Test source asserts an error object with `field: 'snippet'` for a missing canonical snippet.
    - Test source asserts an error object with `field: 'formalObjectId'` for an unknown formal object.
    - Test source asserts the exact reason `Path cannot contain parent traversal` for `../science/paper/science.tex`.
    - Initial test run fails only because `site/src/scripts/validate-math-fixtures.ts` or its exports do not exist yet.
  </acceptance_criteria>
  <done>Failing tests define representative math fixture behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement representative canonical math fixture gate</name>
  <files>site/src/scripts/validate-math-fixtures.ts, site/src/scripts/validate-math-fixtures.test.ts</files>
  <read_first>
    - site/src/scripts/validate-math-fixtures.ts if it already exists
    - site/src/scripts/validate-math-fixtures.test.ts
    - site/src/scripts/validate-formal-registry.ts
    - site/src/data/formal-registry.ts
    - site/src/data/chapters.ts
    - site/src/data/corpus.ts
    - science/paper/science.tex
    - pillars/reliability/paper/reliability_architecture.tex
    - pillars/information/paper/information_architecture.tex
    - pillars/coordination/paper/coordination_architecture.tex
    - pillars/economics/paper/economics_architecture.tex
    - pillars/security/paper/security_architecture.tex
  </read_first>
  <action>Create `site/src/scripts/validate-math-fixtures.ts`. Define default fixtures for `umbrella`, `reliability`, `information`, `coordination`, `economics`, and `security`; select exact snippets that currently exist in each canonical `.tex` file after reading those files, and map each fixture to an existing `formalRegistry` ID owned by the same owner. Implement repository-root resolution from `site/src/scripts` to `/home/prannayag/harness_eng`, reject absolute paths and `..`, check source file existence, check `snippet` substring presence, check `formalObjectId` exists in `formalRegistry`, and if `site/dist` exists check the rendered `href`/anchor target exists without failing when dist has not been built unless fixture explicitly sets `requireRendered: true`. CLI supports `--json`; success prints `OK: {N} math fixtures validated, 0 errors found.`; failure prints `ERRORS: {N} math fixture validation error(s):` plus `formatMathFixtureError`. Do not modify canonical `science/` or `pillars/` files.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-math-fixtures.test.ts && bun run src/scripts/validate-math-fixtures.ts --json</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/scripts/validate-math-fixtures.ts` exports `validateMathFixtures` and `formatMathFixtureError`.
    - Default fixtures include at least 6 entries and owner IDs `umbrella`, `reliability`, `information`, `coordination`, `economics`, and `security`.
    - No file under `science/` or `pillars/` is modified by this task.
    - `bun test src/scripts/validate-math-fixtures.test.ts` exits 0.
    - `bun run src/scripts/validate-math-fixtures.ts --json` emits JSON containing `"ok": true` when fixtures pass.
  </acceptance_criteria>
  <done>Representative math/source fixtures validate canonical snippets, registry IDs, and optional rendered anchors with actionable diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture definitions -> filesystem reads | Fixture paths and snippets direct validator reads of canonical repository files. |
| Canonical source -> release decision | `.tex` content determines whether curated formal claims remain supported. |
| Optional dist HTML -> anchor validation | Generated static HTML is parsed as release evidence when available. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | fixture source paths | mitigate | Reject absolute paths and parent traversal before reading files. |
| T-05-03-02 | Tampering | formal object mapping | mitigate | Require fixture `formalObjectId` to exist in `formalRegistry` and match the fixture owner. |
| T-05-03-03 | Repudiation | math fixture failures | mitigate | Emit entryId, field, path, reason, and nextStep for each missing snippet, invalid registry ID, or missing rendered anchor. |
| T-05-03-04 | Denial of Service | source scanning | accept | Fixture set is fixed and small: umbrella plus five representative pillars; no recursive source scan is introduced. |
| T-05-03-SC | Tampering | npm installs | mitigate | No new packages; use Bun filesystem APIs and existing Vitest tests only. |
</threat_model>

<verification>
Overall checks:
- `cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-math-fixtures.test.ts`
- `cd /home/prannayag/harness_eng/site && bun run src/scripts/validate-math-fixtures.ts --json`
</verification>

<success_criteria>
- Representative umbrella plus multiple pillar math/source fixtures validate against canonical `.tex` files.
- Missing snippets, bad paths, and invalid formal IDs fail with actionable diagnostics.
- Validator can be imported by the release orchestrator in a later wave without adapting error shape.
</success_criteria>

<output>
Create `.planning/phases/05-release-quality-and-static-publication-readiness/05-03-SUMMARY.md` when done.
</output>
