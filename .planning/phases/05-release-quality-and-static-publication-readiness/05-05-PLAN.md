---
phase: 05-release-quality-and-static-publication-readiness
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - site/src/scripts/validate-output-shape.ts
  - site/src/scripts/validate-output-shape.test.ts
autonomous: true
requirements: [QUAL-01, QUAL-05]
user_setup: []
must_haves:
  truths:
    - "Builder can verify deployable static output shape after Astro build and local index generation."
    - "Internal links and hash anchors in generated HTML are checked before publishing."
    - "Required local search, graph, relation, reading-path, corpus, and coverage artifacts exist in dist with required entries."
  artifacts:
    - path: "site/src/scripts/validate-output-shape.ts"
      provides: "Deployable output artifact and internal link/anchor validator"
      exports: ["validateOutputShape", "formatOutputShapeError"]
    - path: "site/src/scripts/validate-output-shape.test.ts"
      provides: "Output-shape, generated artifact, and link/anchor validation tests"
  key_links:
    - from: "site/src/scripts/validate-output-shape.ts"
      to: "site/dist"
      via: "post-build filesystem validation"
      pattern: "dist"
    - from: "site/src/scripts/validate-output-shape.ts"
      to: "site/dist/search-index.json"
      via: "local search artifact check"
      pattern: "search-index\.json"
    - from: "site/src/scripts/validate-output-shape.ts"
      to: "site/dist/coverage-matrix.json"
      via: "coverage artifact check"
      pattern: "coverage-matrix\.json"
---

<objective>
Create the deployable output proof slice: a post-build validator that checks generated HTML, internal links/anchors, Pagefind, local indexes, graph data, citation/search artifacts, coverage JSON, and static deployment shape.

Purpose: Clean release readiness must prove the site output is publishable, not merely that Astro compiled.
Output: `validate-output-shape.ts` with tests and CLI diagnostics for final orchestration.
</objective>

<phase_goal>
**As a** static site builder, **I want to** verify deployable `dist/` output and internal anchors, **so that** publication does not ship missing pages, stale indexes, or broken formal-object links.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-CONTEXT.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-RESEARCH.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-PATTERNS.md
@site/src/scripts/generate-local-indexes.ts
@site/src/scripts/generate-local-indexes.test.ts
@site/src/scripts/generate-coverage-matrix.ts
@site/src/data/corpus.ts
@site/src/data/formal-registry.ts
@site/src/data/relations.ts
@site/src/data/reading-paths.ts

<interfaces>
Output-shape validator target:
- `validateOutputShape(options?)` returns `{ ok: boolean, errors: OutputShapeError[], totals: OutputShapeTotals }`.
- `OutputShapeError` shape is `{ entryId, field, path, reason, nextStep }`.
- Required dist artifacts: `index.html`, `corpus/umbrella/index.html`, at least 12 additional `corpus/*/index.html` pillar pages, `formal-registry/index.html`, `glossary/index.html`, `reading-paths/index.html`, `graph/index.html`, `release-readiness/index.html`, `corpus-index.json`, `search-index.json`, `relation-index.json`, `reading-paths-index.json`, `graph-index.json`, `coverage-matrix.json`, and `pagefind/pagefind.js`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add output-shape validator tests</name>
  <files>site/src/scripts/validate-output-shape.test.ts</files>
  <read_first>
    - site/src/scripts/validate-output-shape.test.ts if it already exists
    - site/src/scripts/generate-local-indexes.test.ts
    - site/src/scripts/generate-coverage-matrix.test.ts
    - site/src/data/corpus.ts
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-CONTEXT.md
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-PATTERNS.md
  </read_first>
  <behavior>
    - A temporary complete dist fixture with required HTML, JSON indexes, coverage JSON, and Pagefind asset passes.
    - Missing `coverage-matrix.json` fails with `field: 'artifact.coverage'`.
    - Missing `pagefind/pagefind.js` fails with `field: 'artifact.pagefind'`.
    - A local link to `missing-page/` fails with `field: 'html.href'`.
    - A local hash link to `#missing-anchor` fails with `field: 'html.fragment'`.
  </behavior>
  <action>Create `site/src/scripts/validate-output-shape.test.ts` using Vitest and temporary directories under `site/dist`. Import not-yet-existing `validateOutputShape` and `formatOutputShapeError`. Build fixture helpers that write minimal HTML files with IDs and local hrefs, minimal JSON index files with required top-level counts/arrays, and `pagefind/pagefind.js`. Tests must cover missing coverage artifact, missing Pagefind asset, missing local page href, and missing hash fragment. Do not require a real Astro build for unit tests.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-output-shape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test source contains required artifact names `coverage-matrix.json`, `search-index.json`, `graph-index.json`, and `pagefind/pagefind.js`.
    - Test source asserts `field: 'artifact.coverage'` for missing coverage JSON.
    - Test source asserts `field: 'html.href'` for a missing local page link.
    - Test source asserts `field: 'html.fragment'` for a missing hash anchor.
    - Initial test run fails only because `validate-output-shape.ts` or its exports do not exist yet.
  </acceptance_criteria>
  <done>Failing tests define deployable output, artifact, and internal link/anchor validation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deployable output and internal link validator</name>
  <files>site/src/scripts/validate-output-shape.ts, site/src/scripts/validate-output-shape.test.ts</files>
  <read_first>
    - site/src/scripts/validate-output-shape.ts if it already exists
    - site/src/scripts/validate-output-shape.test.ts
    - site/src/scripts/generate-local-indexes.ts
    - site/src/scripts/generate-coverage-matrix.ts
    - site/src/data/corpus.ts
    - site/src/data/formal-registry.ts
    - site/src/data/relations.ts
    - site/src/data/reading-paths.ts
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-RESEARCH.md
  </read_first>
  <action>Create `site/src/scripts/validate-output-shape.ts`. Implement `validateOutputShape({ distDir })` defaulting to `site/dist`. Check `distDir` stays under `site/`. Assert required deployable artifacts exist: `index.html`, `corpus/umbrella/index.html`, one `corpus/{slug}/index.html` for every `corpusEntries` slug/id, `formal-registry/index.html`, `glossary/index.html`, `reading-paths/index.html`, `graph/index.html`, `release-readiness/index.html`, `corpus-index.json`, `search-index.json`, `relation-index.json`, `reading-paths-index.json`, `graph-index.json`, `coverage-matrix.json`, and `pagefind/pagefind.js`. Parse generated HTML text for local `href` values; ignore external `http:`, `https:`, `mailto:`, and `tel:` URLs; verify local page targets and hash fragments by collecting `id="..."` and `id='...'` from target HTML. Validate JSON index files contain required count/array fields: `corpus-index.json.entryCount`, `search-index.json.recordCount`, `relation-index.json.recordCount`, `reading-paths-index.json.pathCount`, `graph-index.json`, and `coverage-matrix.json.ownerCount === 13`. CLI supports `--json`; diagnostics use entryId, field, path, reason, nextStep. This implements D-01, D-11, QUAL-01, and QUAL-05 output proof.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-output-shape.test.ts && bun run build:astro && bun run index && bun run src/scripts/generate-coverage-matrix.ts && bun run src/scripts/validate-output-shape.ts --json</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/scripts/validate-output-shape.ts` exports `validateOutputShape` and `formatOutputShapeError`.
    - Source checks `coverage-matrix.json`, `pagefind/pagefind.js`, `release-readiness/index.html`, `graph-index.json`, and `search-index.json`.
    - Source verifies local hash fragments, not only page paths.
    - `bun test src/scripts/validate-output-shape.test.ts` exits 0.
    - After `bun run build:astro && bun run index && bun run src/scripts/generate-coverage-matrix.ts`, `bun run src/scripts/validate-output-shape.ts --json` emits JSON containing `"ok": true`.
  </acceptance_criteria>
  <done>Deployable output validator proves static pages, local indexes, coverage artifacts, Pagefind output, and internal anchors are publishable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated dist -> release validator | Built HTML and JSON artifacts are checked as publishable output. |
| HTML hrefs -> local filesystem paths | Local links in generated pages are resolved to output files and anchors. |
| JSON artifacts -> release decision | Static indexes become proof of search/graph/citation/coverage generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | `distDir` option | mitigate | Require output directory to stay under `site/`; reject traversal/absolute outside paths. |
| T-05-05-02 | Tampering | internal link validation | mitigate | Verify local href target pages and hash fragments from generated HTML IDs. |
| T-05-05-03 | Repudiation | output failures | mitigate | Diagnostics name artifact/page/anchor with field, path, reason, and nextStep. |
| T-05-05-04 | Denial of Service | HTML scanning | accept | Scan is bounded to generated `site/dist/**/*.html` after build; no network or repository-wide scan is added. |
| T-05-05-SC | Tampering | npm installs | mitigate | No new packages; implement with existing Bun/Node filesystem and text parsing only. |
</threat_model>

<verification>
Overall checks:
- `cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-output-shape.test.ts`
- `cd /home/prannayag/harness_eng/site && bun run build:astro && bun run index && bun run src/scripts/generate-coverage-matrix.ts && bun run src/scripts/validate-output-shape.ts --json`
</verification>

<success_criteria>
- Deployable output shape includes all required pages, local indexes, coverage JSON, and Pagefind assets per D-11.
- Internal links and hash anchors are validated after build and indexing per QUAL-01.
- Validator can be composed by the final release command for clean-checkout proof per QUAL-05.
</success_criteria>

<output>
Create `.planning/phases/05-release-quality-and-static-publication-readiness/05-05-SUMMARY.md` when done.
</output>
