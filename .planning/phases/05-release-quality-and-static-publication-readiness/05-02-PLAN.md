---
phase: 05-release-quality-and-static-publication-readiness
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - site/src/scripts/generate-coverage-matrix.ts
  - site/src/scripts/generate-coverage-matrix.test.ts
  - site/src/pages/release-readiness.astro
  - site/src/styles/atlas.css
autonomous: true
requirements: [QUAL-04]
user_setup: []
must_haves:
  truths:
    - "Builder can inspect a compact release-readiness coverage page covering the umbrella framework and all twelve pillars."
    - "Each corpus owner row proves chapter sections, formal objects, concepts, citations, source trails, derivation support/rationale, and discovery/index presence."
    - "Thin or unsupported derivation coverage is visibly labeled with source-grounded rationale instead of presented as full support."
  artifacts:
    - path: "site/src/scripts/generate-coverage-matrix.ts"
      provides: "Typed coverage matrix generation and dist/coverage-matrix.json writer"
      exports: ["buildCoverageMatrix", "writeCoverageMatrix"]
    - path: "site/src/pages/release-readiness.astro"
      provides: "Human-readable release readiness and coverage report page"
    - path: "site/src/styles/atlas.css"
      provides: "Coverage grid and diagnostic page styling using atlas tokens"
    - path: "site/src/scripts/generate-coverage-matrix.test.ts"
      provides: "Coverage generation and output safety regression tests"
  key_links:
    - from: "site/src/scripts/generate-coverage-matrix.ts"
      to: "site/src/data/corpus.ts"
      via: "corpus owner iteration"
      pattern: "corpusEntries"
    - from: "site/src/scripts/generate-coverage-matrix.ts"
      to: "site/src/data/chapters.ts"
      via: "chapter section coverage"
      pattern: "chapterRegistry"
    - from: "site/src/scripts/generate-coverage-matrix.ts"
      to: "site/src/data/formal-registry.ts"
      via: "formal/citation/derivation coverage"
      pattern: "formalRegistry|citations|derivationCoverageByOwner"
    - from: "site/src/pages/release-readiness.astro"
      to: "site/src/scripts/generate-coverage-matrix.ts"
      via: "build-time data import"
      pattern: "buildCoverageMatrix"
---

<objective>
Create the coverage evidence vertical slice: a machine-readable coverage matrix and a human-readable release-readiness report page that prove the full v1 corpus contract for the umbrella framework and all twelve pillars.

Purpose: Release readiness requires visible academic evidence, not a hidden CLI-only pass, and the same data model must feed validators and the reader-facing report to avoid drift.
Output: `/release-readiness/` renders a compact coverage grid and `coverage-matrix.json` can be generated under `site/dist` for release checks.
</objective>

<phase_goal>
**As a** static site builder, **I want to** inspect corpus-wide release coverage evidence, **so that** I can publish the academic book/wiki only when every owner meets the v1 source-grounded content contract.
</phase_goal>

<decision_traceability>
- D-05: The coverage matrix must prove the full v1 contract for the umbrella framework plus all twelve pillars, including required chapter sections, formal objects, concepts, citations, source trails, derivation coverage or rationale, and discovery/index presence.
- D-06: Coverage evidence must be surfaced as a human-readable `/release-readiness/` report/page and as underlying validator-consumable data where useful.
- D-07: Weak canonical support may pass only with source-grounded `Thin source support` or `Not supported by canonical source` rationale and honest reader-facing labels.
- D-08: The report must use a compact coverage grid with drilldown details linking pages, formal anchors, source trails, and diagnostics instead of an undifferentiated table.
</decision_traceability>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-CONTEXT.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-RESEARCH.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-PATTERNS.md
@.planning/phases/05-release-quality-and-static-publication-readiness/05-UI-SPEC.md
@site/src/data/corpus.ts
@site/src/data/chapters.ts
@site/src/data/concepts.ts
@site/src/data/formal-registry.ts
@site/src/data/formal-registry.schema.ts
@site/src/scripts/generate-local-indexes.ts
@site/src/pages/formal-registry/index.astro
@site/src/pages/inventory.astro
@site/src/styles/atlas.css

<interfaces>
Coverage matrix target shape:
- `CoverageMatrix.generatedBy` is exactly `site/src/scripts/generate-coverage-matrix.ts`.
- `CoverageMatrix.ownerCount` is 13.
- `OwnerCoverage` includes `ownerId`, `ownerTitle`, `chapterHref`, `chapterSections`, `formalObjectCount`, `conceptCount`, `citationCount`, `sourceTrailCount`, `derivationCoverage`, `discoveryPresence`, and `diagnostics`.
- `derivationCoverage` uses text labels `Supported`, `Thin source support`, `Not supported by canonical source`, or `Missing`.
- All generated file writes must stay inside `site/` and default to `site/dist/coverage-matrix.json`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coverage matrix tests and generator</name>
  <files>site/src/scripts/generate-coverage-matrix.ts, site/src/scripts/generate-coverage-matrix.test.ts</files>
  <read_first>
    - site/src/scripts/generate-coverage-matrix.ts if it already exists
    - site/src/scripts/generate-coverage-matrix.test.ts if it already exists
    - site/src/scripts/generate-local-indexes.ts
    - site/src/scripts/generate-local-indexes.test.ts
    - site/src/data/corpus.ts
    - site/src/data/chapters.ts
    - site/src/data/concepts.ts
    - site/src/data/formal-registry.ts
    - site/src/data/formal-registry.schema.ts
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-CONTEXT.md
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-PATTERNS.md
  </read_first>
  <behavior>
    - Real data builds a matrix with `ownerCount` equal to 13 and rows for umbrella plus all twelve pillars per D-05.
    - Every owner row reports required chapter sections, formal object count, concept count, citation count, source trail count, derivation coverage label/rationale, and discovery presence.
    - Thin or unsupported derivation entries include the exact labels `Thin source support` or `Not supported by canonical source` per D-07.
    - `writeCoverageMatrix({ outputDir: '../dist' })` throws `Output directory must stay inside site/`.
  </behavior>
  <action>Create `site/src/scripts/generate-coverage-matrix.test.ts` first, then implement `site/src/scripts/generate-coverage-matrix.ts`. Build coverage from `corpusEntries`, `chapterRegistry`, `conceptRegistry`, `formalRegistry`, `citations`, `derivationCoverageByOwner`, `readingPaths`, `relationRecords`, and `buildDiscoverySearchIndex()`/`buildGraphIndex()` outputs. Required chapter section keys are exactly `problem`, `coreModel`, `keyNotation`, `definitions`, `formalClaims`, `derivationContext`, `interpretation`, `relatedPillars`, `citations`, and `sourceTrail`. Implement `buildCoverageMatrix()`, `writeCoverageMatrix(options)`, and `writeAllCoverageArtifacts(options)` if useful. Use the output safety pattern from `generate-local-indexes.ts`; default output file is `coverage-matrix.json`. Diagnostics must use fields `gate`, `entryId`, `field`, `path`, `reason`, and `nextStep` so the release orchestrator can consume them later. Do not duplicate hard-coded owner lists outside `expectedCorpusIds`/`corpusEntries`.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/scripts/generate-coverage-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/scripts/generate-coverage-matrix.ts` exports `buildCoverageMatrix` and `writeCoverageMatrix`.
    - Generated matrix has `generatedBy: 'site/src/scripts/generate-coverage-matrix.ts'`.
    - Tests assert `ownerCount` is `13`.
    - Tests assert output safety throws `Output directory must stay inside site/` for `../dist`.
    - Tests assert the exact labels `Thin source support` and `Not supported by canonical source` are represented for non-full derivation support states when present in source data.
  </acceptance_criteria>
  <done>Coverage data model proves umbrella-plus-twelve-pillar release contract and writes bounded machine-readable coverage evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render human-readable release readiness coverage page</name>
  <files>site/src/pages/release-readiness.astro, site/src/styles/atlas.css, site/src/scripts/generate-coverage-matrix.test.ts</files>
  <read_first>
    - site/src/pages/release-readiness.astro if it already exists
    - site/src/pages/formal-registry/index.astro
    - site/src/pages/inventory.astro
    - site/src/styles/atlas.css
    - site/src/scripts/generate-coverage-matrix.ts
    - site/src/scripts/generate-coverage-matrix.test.ts
    - .planning/phases/05-release-quality-and-static-publication-readiness/05-UI-SPEC.md
  </read_first>
  <behavior>
    - `/release-readiness/` renders one top summary panel before coverage details per UI-SPEC.
    - Page includes the primary action text `Run Release Check`, print affordance `Print research handout`, and proof labels `Clean-checkout proof` and `Deployable static output`.
    - Compact grid columns include owner, chapter sections, formal objects, concepts, citations, source trails, derivation coverage/rationale, discovery/index presence, and diagnostics per D-08.
    - Owner rows link to chapters, formal anchors when available, source trails, and diagnostics anchors.
  </behavior>
  <action>Create `site/src/pages/release-readiness.astro` using the existing report shell style from `formal-registry/index.astro` and `inventory.astro`. Import `buildCoverageMatrix` from `../scripts/generate-coverage-matrix` and render the matrix at build time; do not fetch JSON at runtime. The header eyebrow is `Release readiness evidence`; the page title is `Release readiness`; the empty state copy is `No release diagnostics yet`; the primary CTA visible text is `Run Release Check`; secondary labels include `Review Coverage Matrix`, `Print research handout`, `Clean-checkout proof`, and `Deployable static output`. Render a compact semantic table or list-grid with required column labels: `Owner`, `Chapter sections`, `Formal objects`, `Concepts`, `Citations`, `Source trails`, `Derivation coverage`, `Discovery/index presence`, and `Diagnostics`. Add atlas CSS classes prefixed `release-readiness__` and `coverage-matrix__`; use existing CSS variables and UI-SPEC colors only. Labels for limited support must be exactly `Thin source support — release allowed only with the recorded source-grounded limitation.` and `Not supported by canonical source — do not present as a full derivation.` per D-07/UI-SPEC. Include accessible disclosure controls or native links for drilldown; no client-side graph interaction.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run build:astro && test -f dist/release-readiness/index.html && grep -q "Release readiness" dist/release-readiness/index.html && grep -q "Run Release Check" dist/release-readiness/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/pages/release-readiness.astro` contains `Release readiness evidence` and `Run Release Check`.
    - Built `site/dist/release-readiness/index.html` contains `Owner`, `Chapter sections`, `Formal objects`, `Concepts`, `Citations`, `Source trails`, `Derivation coverage`, `Discovery/index presence`, and `Diagnostics`.
    - Built page contains `Print research handout`, `Clean-checkout proof`, and `Deployable static output`.
    - `site/src/styles/atlas.css` contains selectors beginning `.release-readiness__` and `.coverage-matrix__`.
    - Page source does not contain runtime `fetch(` for `coverage-matrix.json`.
  </acceptance_criteria>
  <done>Reader-facing release readiness page exposes compact, source-grounded coverage evidence with accessible drilldowns and required copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry data -> generated coverage matrix | Corpus/chapter/formal/discovery registry values become release evidence and static JSON. |
| Generated coverage -> Astro page | Machine-readable coverage is rendered as human publication evidence. |
| Generator -> filesystem | Coverage writer creates artifacts under `site/dist`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | `writeCoverageMatrix` output path | mitigate | Reuse `assertInsideSite` output bound check and test `../dist` rejection. |
| T-05-02-02 | Information Disclosure | release-readiness page | accept | Page renders source trails and repository-relative paths already exposed by the academic site; no secrets or private data sources are added. |
| T-05-02-03 | Repudiation | coverage diagnostics | mitigate | Store per-owner diagnostics with gate, entryId, field, path, reason, and nextStep rather than aggregate-only pass/fail. |
| T-05-02-04 | Tampering | derivation support labels | mitigate | Use exact limited-support labels and source-grounded rationale so unsupported claims are not promoted as full derivations. |
| T-05-02-SC | Tampering | npm installs | mitigate | No new package installs; use existing Astro/Bun/Vitest/Zod stack only. |
</threat_model>

<verification>
Overall checks:
- `cd /home/prannayag/harness_eng/site && bun test src/scripts/generate-coverage-matrix.test.ts`
- `cd /home/prannayag/harness_eng/site && bun run build:astro`
- `cd /home/prannayag/harness_eng/site && bun run src/scripts/generate-coverage-matrix.ts`
</verification>

<success_criteria>
- Coverage matrix covers exactly 13 owners: umbrella plus all twelve pillars.
- Coverage evidence includes chapter sections, formal objects, concepts, citations, source trails, derivation coverage/rationale, discovery/index presence, and diagnostics per D-05.
- `/release-readiness/` renders a compact human-readable report and required UI-SPEC copy per D-06 and D-08.
- Limited derivation support states are labeled honestly per D-07.
</success_criteria>

<output>
Create `.planning/phases/05-release-quality-and-static-publication-readiness/05-02-SUMMARY.md` when done.
</output>
