---
phase: 03-curated-corpus-chapters-and-formal-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/data/formal-registry.schema.ts
  - site/src/data/formal-registry.ts
  - site/src/data/chapters.ts
  - site/src/data/concepts.ts
  - site/src/scripts/validate-formal-registry.ts
  - site/src/scripts/validate-formal-registry.test.ts
  - site/src/data/formal-registry.test.ts
  - site/package.json
autonomous: true
requirements: [FORM-01, FORM-03, FORM-04, FORM-05, FORM-06, FORM-07, FORM-08, FORM-09, FORM-10]
must_haves:
  truths:
    - "Builder can validate a structured Phase 3 registry before Astro renders curated content from typed data per D-05."
    - "Umbrella formal content has stable semantic IDs per D-06, broad registry kinds per D-07, source trails, citations, and concept links."
    - "Every corpus owner has a build-safe minimal chapter record before route expansion, so static generation never depends on later enrichment plans."
    - "Invalid duplicate IDs, owner IDs, paths, source tiers, chapter sections, curation statuses, broken anchors, missing required fields, or owned relation targets fail validation per D-08."
  artifacts:
    - path: "site/src/data/formal-registry.schema.ts"
      provides: "Zod schemas and TypeScript types for formal objects, citations, source trails, chapters, and concepts"
      exports: ["parseFormalRegistry", "parseChapterRegistry", "parseConceptRegistry"]
    - path: "site/src/scripts/validate-formal-registry.ts"
      provides: "Strict Phase 3 validation gate"
      exports: ["validateFormalRegistry", "formatFormalRegistryError"]
    - path: "site/src/data/formal-registry.ts"
      provides: "Registry-first umbrella formal object seed plus all-owner build-safe minimal formal records"
      contains: "umbrella.harness-architecture"
  key_links:
    - from: "site/package.json"
      to: "site/src/scripts/validate-formal-registry.ts"
      via: "validate script chain"
      pattern: "validate-formal-registry"
    - from: "site/src/data/chapters.ts"
      to: "site/src/data/formal-registry.ts"
      via: "chapter object ID references"
      pattern: "formalObjectIds"
---

## Phase Goal

**As a** harness architecture researcher, **I want to** read source-grounded umbrella and pillar chapters with consistent formal objects, derivations, citations, glossary concepts, and source trails, **so that** I can navigate the corpus without losing canonical rigor.

<objective>
Create the registry-first vertical slice for the umbrella framework plus validated build-safe minimal chapter records for all thirteen corpus owners that downstream rendering plans can consume.

Purpose: This implements D-05 through D-08 before page prose can drift into unvalidated IDs, unlabeled sources, or broken relations.
Output: Registry schemas, umbrella curated seed data, all-owner minimal chapter/formal/concept records with curation status, formal validation CLI, tests, and `bun run validate` integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/prannayag/harness_eng/.planning/PROJECT.md
@/home/prannayag/harness_eng/.planning/ROADMAP.md
@/home/prannayag/harness_eng/.planning/REQUIREMENTS.md
@/home/prannayag/harness_eng/.planning/phases/03-curated-corpus-chapters-and-formal-registry/03-CONTEXT.md
@/home/prannayag/harness_eng/.planning/phases/03-curated-corpus-chapters-and-formal-registry/03-RESEARCH.md
@/home/prannayag/harness_eng/.planning/phases/03-curated-corpus-chapters-and-formal-registry/03-PATTERNS.md
@/home/prannayag/harness_eng/.planning/phases/03-curated-corpus-chapters-and-formal-registry/03-UI-SPEC.md
@/home/prannayag/harness_eng/site/src/data/corpus.schema.ts
@/home/prannayag/harness_eng/site/src/data/corpus.ts
@/home/prannayag/harness_eng/site/src/scripts/validate-corpus.ts
@/home/prannayag/harness_eng/site/package.json
@/home/prannayag/harness_eng/science/paper/science.tex
@/home/prannayag/harness_eng/science/paper/science.bib
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing registry and validation tests for the umbrella vertical slice</name>
  <files>site/src/data/formal-registry.test.ts, site/src/scripts/validate-formal-registry.test.ts</files>
  <read_first>
    - site/src/data/formal-registry.test.ts if it exists
    - site/src/scripts/validate-formal-registry.test.ts if it exists
    - site/src/data/corpus.test.ts
    - site/src/scripts/validate-corpus.test.ts
    - site/src/data/corpus.schema.ts
    - site/src/scripts/validate-corpus.ts
    - .planning/phases/03-curated-corpus-chapters-and-formal-registry/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test real Phase 3 data parses and includes owner `umbrella` with object ID `umbrella.harness-architecture`.
    - Test every `expectedCorpusIds` owner has a build-safe minimal chapter record, owner ID, source trail, curation status, and at least one schema-valid minimal formal/concept record before route expansion.
    - Test stable ID regex accepts `reliability.compound-error-bound` and rejects paper-number/line-number IDs such as `theorem-1` and `science.tex:42` per D-06.
    - Test validation fails duplicate formal IDs, invalid owner IDs, missing source paths, archive-as-canonical paths, missing required chapter sections, and broken `relatedObjectIds` targets per D-08.
    - Test source tier labels include exactly `canonical`, `supporting-research`, `synthesis-review`, and `archived-provenance` per D-12.
  </behavior>
  <action>Create Vitest tests before implementation. Use clone-and-mutate fixtures following `validate-corpus.test.ts`. Do not add packages. Assert exact diagnostics include `Duplicate formal object id`, `Invalid owner id`, `Required source path does not exist`, `Archive paths cannot be canonical sources`, `Missing required chapter section`, `Invalid curation status`, and `Relation target does not exist`. Add an all-owner build-safety assertion per the revision dependency fix: every `expectedCorpusIds` owner must have a schema-valid chapter record with owner ID, canonical source trail, `curationStatus` such as `minimal`/`curated`, and enough section/object/concept data for `/corpus/[slug]/` to render before Plans 03-03/03-04 enrich pillars. The tests must prove D-05, D-06, D-07, D-08, D-10, D-12, D-13, and D-16 are executable contracts.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/data/formal-registry.test.ts src/scripts/validate-formal-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test files contain `umbrella.harness-architecture` and `reliability.compound-error-bound`.
    - Test files contain assertions for duplicate IDs, invalid owners, missing source paths, archive-as-canonical paths, missing chapter sections, invalid curation statuses, all-owner minimal chapter records, and broken relation targets.
    - Initial test run fails because `formal-registry.schema.ts` or `validate-formal-registry.ts` does not yet exist.
  </acceptance_criteria>
  <done>Failing tests define the Phase 3 registry-first contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed umbrella registry data, all-owner minimal records, and strict validation gate</name>
  <files>site/src/data/formal-registry.schema.ts, site/src/data/formal-registry.ts, site/src/data/chapters.ts, site/src/data/concepts.ts, site/src/scripts/validate-formal-registry.ts, site/package.json</files>
  <read_first>
    - site/src/data/formal-registry.schema.ts if it exists
    - site/src/data/formal-registry.ts if it exists
    - site/src/data/chapters.ts if it exists
    - site/src/data/concepts.ts if it exists
    - site/src/scripts/validate-formal-registry.ts if it exists
    - site/package.json
    - site/src/data/corpus.schema.ts
    - site/src/data/corpus.ts
    - site/src/scripts/validate-corpus.ts
    - science/paper/science.tex
    - science/paper/science.bib
  </read_first>
  <action>Implement `formalObjectKindSchema` with `definition`, `assumption`, `theorem`, `proposition`, `lemma`, `corollary`, `equation`, `derivation`, and `citation`; `sourceTierSchema` with `canonical`, `supporting-research`, `synthesis-review`, and `archived-provenance`; a finite `curationStatusSchema` such as `minimal` and `curated`; stable IDs matching owner plus semantic slug per D-06; and parsed exports `formalRegistry`, `chapterRegistry`, and `conceptRegistry` per D-05/D-07. Seed a real umbrella vertical slice with chapter sections `problem`, `coreModel`, `keyNotation`, `definitions`, `formalClaims`, `derivationContext`, `interpretation`, `relatedPillars`, `citations`, and `sourceTrail`, backed by `science/paper/science.tex`, `science/paper/science.pdf`, and `science/paper/science.bib` per D-02/D-09/D-10. Also create validated build-safe minimal chapter records for all twelve pillar owners using `corpusEntries` canonical `.tex`/`.pdf`/`.bib` paths, owner-prefixed minimal formal object IDs, source trails, placeholder/minimal curation status, and concise section text that labels the record incomplete rather than falsely curated. This is not a scope reduction: Plans 03-03/03-04 must enrich these records to full D-01 curated chapters, and Plan 03-05 must fail if any `minimal`/placeholder status remains. Implement `validateFormalRegistry()` using the collect-all-errors pattern from `validate-corpus.ts`, checking duplicate IDs, expected owners, source path existence, archive canonical misuse, source tiers, curation status values, required fields, required chapter sections, concept targets, citation targets, and relation targets owned by Phase 3 per D-08. Chain `bun run src/scripts/validate-formal-registry.ts` after `validate-corpus.ts` in `site/package.json` without adding dependencies.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun run test -- src/data/formal-registry.test.ts src/scripts/validate-formal-registry.test.ts && bun run validate</automated>
  </verify>
  <acceptance_criteria>
    - `site/src/data/formal-registry.schema.ts` exports `parseFormalRegistry`, `parseChapterRegistry`, and `parseConceptRegistry`.
    - `site/src/data/formal-registry.ts` contains `umbrella.harness-architecture` with owner `umbrella` and a canonical source trail to `science/paper/science.tex`.
    - `site/src/data/chapters.ts` contains a schema-valid chapter record for every `expectedCorpusIds` owner before Plan 03-02 expands static route rendering.
    - Every non-umbrella owner has owner ID, canonical source trail, `curationStatus: minimal` or equivalent placeholder-curation status, and build-safe section/object/concept references that validation accepts.
    - `site/src/data/chapters.ts` contains all ten section keys required by D-01 for the umbrella opening-framework chapter or a typed umbrella equivalent that still includes definitions, claims, derivation context, citations, and source trail.
    - `site/src/scripts/validate-formal-registry.ts` exits non-zero for invalid fixtures and emits collect-all diagnostics.
    - `site/package.json` `validate` script runs both `validate-corpus.ts` and `validate-formal-registry.ts`.
  </acceptance_criteria>
  <done>Registry schema, umbrella seed data, and strict validation pass the tests and are part of the standard validation command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated data -> static renderer | Authored TypeScript data becomes public HTML anchors, links, and prose. |
| repository paths -> source links | Registry paths become GitHub/source trail URLs. |
| validation CLI -> build gate | Build trust depends on validation catching misleading or broken formal metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | formal IDs | mitigate | Validate unique semantic IDs and render anchors from registry IDs only. |
| T-03-01-02 | Tampering | source paths | mitigate | Reject absolute paths, parent traversal, missing files, and archive paths used as canonical. |
| T-03-01-03 | Repudiation | source tiers | mitigate | Require source tier enum and visible source trail IDs on every formal object and chapter section. |
| T-03-01-04 | Tampering | relation targets | mitigate | Validate `relatedObjectIds`, `conceptIds`, and citation references before build. |
| T-03-01-SC | Tampering | npm installs | accept | No package install task exists; existing dependencies are used only. |
</threat_model>

<verification>
Run `cd /home/prannayag/harness_eng/site && bun run test -- src/data/formal-registry.test.ts src/scripts/validate-formal-registry.test.ts && bun run validate`.
</verification>

<success_criteria>
A real umbrella formal chapter slice exists in typed data, every corpus owner has a validated build-safe minimal record, invalid registry data fails the build, and downstream routes can import registry/chapter/concept data by owner without inventing IDs, fallback content, or source paths.
</success_criteria>

<output>
Create `/home/prannayag/harness_eng/.planning/phases/03-curated-corpus-chapters-and-formal-registry/03-01-SUMMARY.md` when done.
</output>
