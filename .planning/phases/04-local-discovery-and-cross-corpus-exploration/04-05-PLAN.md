---
phase: 04-local-discovery-and-cross-corpus-exploration
plan: 05
type: execute
wave: 5
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - site/package.json
  - site/src/scripts/validate-discovery.ts
  - site/src/scripts/validate-discovery.test.ts
  - site/src/scripts/generate-local-indexes.ts
  - site/src/scripts/generate-local-indexes.test.ts
  - site/src/data/discovery.test.ts
  - site/src/pages/corpus/[slug].astro
  - site/src/styles/atlas.css
autonomous: true
requirements:
  - BOOK-04
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05
  - DISC-06
  - QUAL-03
must_haves:
  truths:
    - "D-01: Final validation confirms reading paths preserve route-map presentation rather than plain essay/index presentation."
    - "D-02: Final validation confirms reading paths preserve branching route structures instead of single fixed sequences."
    - "D-03: Final validation confirms route stops expose why-it-matters guidance plus direct target links."
    - "D-04: Final validation confirms the five required reading paths are curated structured data, not generated first from relation metadata."
    - "D-05: Final validation confirms local search balances pages, pillar names, formal objects, concepts, citations, and reading paths."
    - "D-06: Final validation confirms search result grouping by result type."
    - "D-07: Final validation confirms formal-object hits expose kind, stable ID/title, owner context, snippet, and direct anchor before click."
    - "D-08: Final validation confirms representative search checks assert expected pages or formal-object anchors, not only non-empty results."
    - "D-09: Final validation confirms the relationship taxonomy spans conceptual, source/provenance, and learning-path relationships."
    - "D-10: Final validation confirms relation types remain extensible through validated/documented records."
    - "D-11: Final validation confirms typed related-link sections render on chapter and formal-object pages without replacing source trails, formal source tiers, stable IDs, or anchor links."
    - "D-12: Final validation confirms relation metadata checks type definitions, source IDs, target IDs, directionality, and display labels."
    - "D-13: Final validation confirms graph context exposes local neighborhoods and a high-level global overview."
    - "D-14: Final validation confirms graph surfaces remain static generated pages/diagrams/HTML with clickable links and no runtime graph database/heavy client engine."
    - "D-15: Final validation confirms local graph neighborhoods emphasize path discovery and route-map next readings."
    - "D-16: Final validation confirms graph checks cover node IDs, relation targets/types, required labels, and renderability."
    - "The one-command build/index workflow generates local search and graph artifacts without a hosted service or runtime database."
    - "Chapter pages, formal-object sections, reading paths, search, related links, and graph pages share the Phase 4 UI contract and remain accessible on narrow screens."
    - "All Phase 4 requirements are covered by automated tests or validation checks."
  artifacts:
    - path: "site/package.json"
      provides: "Script wiring for discovery validation in existing build/validate workflow"
    - path: "site/src/scripts/validate-discovery.ts"
      provides: "End-to-end Phase 4 validation gate"
    - path: "site/src/scripts/generate-local-indexes.ts"
      provides: "Combined local index/artifact generation"
    - path: "site/src/data/discovery.test.ts"
      provides: "Source/data contract coverage audit tests"
  key_links:
    - from: "site/package.json"
      to: "site/src/scripts/validate-discovery.ts"
      via: "validation script command"
      pattern: "validate-discovery"
    - from: "site/package.json"
      to: "site/src/scripts/generate-local-indexes.ts"
      via: "index generation command"
      pattern: "generate-local-indexes"
    - from: "site/src/data/discovery.test.ts"
      to: "site/src/components/discovery/RelatedLinks.astro"
      via: "D-11 contract assertions for chapter and formal-object relation rows"
      pattern: "Related links"
---

<objective>
Create the final Phase 4 hardening vertical slice: integrate discovery validation and generated artifacts into the existing site workflow, close requirement coverage, and enforce the UI/accessibility/search quality gates.

Purpose: Prior plans add each discovery surface. This plan makes the phase shippable by wiring validation/index generation and proving the full static discovery contract across BOOK-04, DISC-01 through DISC-06, and QUAL-03.
Output: Package script wiring, consolidated validation/tests, generated index coverage, and final source/UI contract assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-CONTEXT.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-RESEARCH.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-PATTERNS.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-UI-SPEC.md
@site/package.json
@site/src/scripts/validate-formal-registry.ts
@site/src/scripts/validate-discovery.ts
@site/src/scripts/generate-local-indexes.ts
@site/src/data/discovery.test.ts
@site/src/scripts/validate-discovery.test.ts
@site/src/scripts/generate-local-indexes.test.ts
@site/src/components/discovery/RelatedLinks.astro
@site/src/components/formal/FormalObjectList.astro
@site/src/pages/corpus/[slug].astro
@site/src/styles/atlas.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire discovery validation and index generation into site scripts</name>
  <files>site/package.json, site/src/scripts/validate-discovery.ts, site/src/scripts/generate-local-indexes.ts, site/src/scripts/validate-discovery.test.ts, site/src/scripts/generate-local-indexes.test.ts</files>
  <read_first>
    - site/package.json for existing Bun scripts, validate/build/index workflow, and Pagefind command.
    - site/src/scripts/validate-discovery.ts and tests from prior plans.
    - site/src/scripts/generate-local-indexes.ts and tests from prior plans.
    - site/src/scripts/validate-formal-registry.ts for existing validation command behavior.
    - .planning/phases/04-local-discovery-and-cross-corpus-exploration/04-RESEARCH.md Standard Stack and Package Legitimacy Audit: no new packages; vitest is existing only.
  </read_first>
  <behavior>
    - Test that all generated local indexes can be built by one script invocation and contain corpus, search, relations, reading paths, and graph sections/files.
    - Test that validation fails when discovery fixtures are malformed and passes for real data.
    - Source/assertion test that `package.json` includes a discovery validation command and the existing validate workflow invokes it.
  </behavior>
  <action>Update `site/package.json` scripts so the existing validation workflow runs `validate-discovery.ts` alongside formal registry/provenance validation, and the existing local index generation command writes corpus, search, reading-path, relation, and graph artifacts. Do not add dependencies or change package manager. Ensure `generate-local-indexes.ts` logs generated files to stderr and exits non-zero on failure, preserving existing CLI style. Ensure `validate-discovery.ts --json` returns totals for paths, relations, graph nodes/neighborhoods, search fixtures, and errors.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/scripts/validate-discovery.test.ts src/scripts/generate-local-indexes.test.ts && bun run validate</automated>
  </verify>
  <acceptance_criteria>
    - `bun run validate` includes discovery validation and fails on invalid relation/path/graph/search fixture metadata.
    - Local index generation writes all Phase 4 JSON artifacts under `site/dist`.
    - No package install or dependency change is introduced.
    - CLI errors go to stderr and exit non-zero on failure.
  </acceptance_criteria>
  <done>
    - `bun run validate` includes discovery validation and fails on invalid relation/path/graph/search fixture metadata.
    - Local index generation writes all Phase 4 JSON artifacts under `site/dist`.
    - No package install or dependency change is introduced.
    - CLI errors go to stderr and exit non-zero on failure.

  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add final source/UI contract coverage for Phase 4 surfaces</name>
  <files>site/src/data/discovery.test.ts, site/src/scripts/validate-discovery.test.ts, site/src/pages/corpus/[slug].astro, site/src/styles/atlas.css</files>
  <read_first>
    - site/src/data/discovery.test.ts, site/src/scripts/validate-discovery.test.ts for current coverage.
    - site/src/components/discovery/RelatedLinks.astro for typed related-link labels, target family/type rendering, rationale/context copy, direct links, and empty state.
    - site/src/components/formal/FormalObjectList.astro for formal-object D-11 integration and preservation of source tiers, stable IDs, and anchor links.
    - site/src/pages/corpus/[slug].astro for final integration of related links and graph local context CTAs.
    - site/src/styles/atlas.css for Phase 4 responsive/focus styles.
    - .planning/phases/04-local-discovery-and-cross-corpus-exploration/04-CONTEXT.md all decisions D-01 through D-16.
    - .planning/phases/04-local-discovery-and-cross-corpus-exploration/04-UI-SPEC.md all Phase 4 interaction/accessibility/copy requirements.
    - .planning/REQUIREMENTS.md BOOK-04, DISC-01 through DISC-06, QUAL-03 descriptions.
  </read_first>
  <behavior>
    - Test/source assertions map D-01 through D-16 to implemented files or validation checks.
    - Test/source assertions confirm UI-SPEC required copy appears: search placeholder, group labels, `Related links`, route-stop labels, graph CTAs/explanation, and empty/error/no-results copy.
    - Test/source assertions confirm focus styles, 44px minimum interactive targets, and max-width 720px one-column collapse exist for search/path/related/graph classes.
    - Test/source assertions confirm corpus pages include related links and graph local context affordance without removing source trails and BookFooterNav.
    - Test/source assertions confirm formal-object related-link rendering exists and exposes readable labels, target type/family, rationale/context, and direct links without removing formal source tiers, stable IDs, or anchor links.
  </behavior>
  <action>Add a final Phase 4 contract test block to existing discovery tests instead of creating a prose checklist. Assert every locked CONTEXT decision D-01 through D-16 has a concrete implementation hook: reading-path data/pages for D-01-D-04, search/index/UI/fixtures for D-05-D-08, relation taxonomy/rendering/validation for D-09-D-12, graph pages/artifacts/validation for D-13-D-16. For D-11 specifically, assert both chapter related links and formal-object-context related links render typed sections with readable relation labels, target title, target family/type, rationale/context, and direct links, and assert SourceLinkPanel, BookFooterNav, FormalObjectList source tiers, stable IDs, and anchor links remain present. Assert required UI-SPEC copy and accessibility/focus/mobile CSS selectors are present. If the graph local-context CTA is not yet visible on corpus pages, add it near related links without hiding source trails.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/data/discovery.test.ts src/scripts/validate-discovery.test.ts && bun run build && bun run index</automated>
  </verify>
  <acceptance_criteria>
    - Automated tests trace all Phase 4 locked decisions D-01 through D-16 to code or validation hooks.
    - D-11 tests prove typed related-link sections exist for both chapter pages and formal-object contexts.
    - Formal-object related-link assertions cover readable labels, target type/family, rationale/context, direct links, and preservation of formal source tiers/stable IDs/anchor links.
    - Required UI-SPEC copy and accessibility patterns are source-asserted.
    - Corpus pages preserve source trails and BookFooterNav while adding related-link and local-context navigation.
    - `bun run build && bun run index` succeeds after validation wiring.
  </acceptance_criteria>
  <done>
    - Automated tests trace all Phase 4 locked decisions D-01 through D-16 to code or validation hooks.
    - D-11 tests prove typed related-link sections exist for both chapter pages and formal-object contexts.
    - Formal-object related-link assertions cover readable labels, target type/family, rationale/context, direct links, and preservation of formal source tiers/stable IDs/anchor links.
    - Required UI-SPEC copy and accessibility patterns are source-asserted.
    - Corpus pages preserve source trails and BookFooterNav while adding related-link and local-context navigation.
    - `bun run build && bun run index` succeeds after validation wiring.

  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer scripts -> release artifacts | Build/validate/index commands decide whether discovery artifacts ship. |
| Static discovery surfaces -> readers | Readers trust search, relationship, and graph navigation as source-grounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | `site/package.json` validation workflow | mitigate | Wire discovery validation into `bun run validate`; malformed metadata must fail the standard workflow. |
| T-04-15 | Repudiation | Requirement/decision coverage | mitigate | Add source-level tests mapping D-01 through D-16 and requirements to implementation hooks, including D-11 chapter and formal-object related-link surfaces, so omissions are visible. |
| T-04-16 | Denial of Service | Build/index workflow | mitigate | Keep generation static and bounded to JSON/HTML artifacts; no runtime service or graph database. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Existing vitest SUS status is accepted only as an existing dependency; dependency changes require a blocking legitimacy review. |
</threat_model>

<verification>
Run `cd /home/prannayag/harness_eng/site && bun test && bun run validate && bun run build && bun run index`. Confirm generated artifacts include corpus, search, relations, reading paths, and graph outputs; confirm D-11 assertions cover both chapter and formal-object related-link rendering; and confirm no canonical `science/` or `pillars/` files were modified.
</verification>

<success_criteria>
Phase 4 is ready for execution verification: all requirements are represented in scripts/tests/build output, all locked decisions D-01 through D-16 are traceable, and the site exposes static reading paths, local search, typed related links on chapter and formal-object contexts, and graph-style context with validation gates.
</success_criteria>

<output>
Create `.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-05-SUMMARY.md` when done.
</output>
