---
phase: 04-local-discovery-and-cross-corpus-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/data/discovery.schema.ts
  - site/src/data/reading-paths.ts
  - site/src/data/discovery.test.ts
  - site/src/components/discovery/ReadingPathMap.astro
  - site/src/pages/reading-paths/[slug].astro
  - site/src/styles/atlas.css
autonomous: true
requirements:
  - BOOK-04
  - DISC-02
  - DISC-04
must_haves:
  truths:
    - "D-04: User can browse five curated structured-data reading paths: building a harness, scaling multi-agent work, cost/latency, production hardening, and AI code degradation."
    - "D-01: Each reading path feels like a route map, not a plain essay or compact index."
    - "D-02: Each reading path presents multiple route branches instead of a single fixed sequence."
    - "D-03: Each stop explains why it matters and links directly to a chapter, concept, formal object, citation, or anchor."
  artifacts:
    - path: "site/src/data/discovery.schema.ts"
      provides: "Typed schemas for reading-path records and target references"
      exports: ["readingPathSchema", "parseReadingPaths"]
    - path: "site/src/data/reading-paths.ts"
      provides: "Curated five-path route map registry"
      contains: "building-a-harness"
    - path: "site/src/components/discovery/ReadingPathMap.astro"
      provides: "Accessible branching route-map renderer"
    - path: "site/src/pages/reading-paths/[slug].astro"
      provides: "Static route for one reading path per slug"
  key_links:
    - from: "site/src/pages/reading-paths/[slug].astro"
      to: "site/src/data/reading-paths.ts"
      via: "getStaticPaths over readingPaths"
      pattern: "readingPaths\.map"
    - from: "site/src/components/discovery/ReadingPathMap.astro"
      to: "route stop hrefs"
      via: "direct Open stop links"
      pattern: "Open stop"
---

<objective>
Create the first Phase 4 vertical slice: curated, searchable, static reading-path route maps for the five required research themes.

Purpose: BOOK-04 is the user-facing entry point for discovery. This plan implements D-01 through D-04 exactly: route-map feel, branching paths, lightweight stop guidance, and curated structured data for all five required paths.
Output: Reading path schemas, curated path data, route-map component, static reading-path pages, and tests proving the five-path contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-CONTEXT.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-RESEARCH.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-PATTERNS.md
@.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-UI-SPEC.md
@site/src/data/corpus.ts
@site/src/data/book-spine.ts
@site/src/data/formal-registry.schema.ts
@site/src/data/formal-registry.ts
@site/src/data/concepts.ts
@site/src/data/chapters.ts
@site/src/components/AtlasConstellation.astro
@site/src/pages/corpus/[slug].astro
@site/src/styles/atlas.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the curated reading-path contract</name>
  <files>site/src/data/discovery.schema.ts, site/src/data/reading-paths.ts, site/src/data/discovery.test.ts</files>
  <read_first>
    - site/src/data/discovery.schema.ts if it already exists; otherwise read site/src/data/formal-registry.schema.ts for Zod schema style.
    - site/src/data/reading-paths.ts if it already exists; otherwise read site/src/data/book-spine.ts for typed curated navigation data style.
    - site/src/data/corpus.ts, site/src/data/chapters.ts, site/src/data/concepts.ts, site/src/data/formal-registry.ts for valid target IDs and href construction.
    - .planning/phases/04-local-discovery-and-cross-corpus-exploration/04-CONTEXT.md decisions D-01, D-02, D-03, D-04.
    - .planning/phases/04-local-discovery-and-cross-corpus-exploration/04-UI-SPEC.md Reading Paths interaction contract.
  </read_first>
  <behavior>
    - Test that exactly five reading paths exist with slugs for building a harness, scaling multi-agent work, cost/latency, production hardening, and AI code degradation.
    - Test that every path has at least two branches and every branch has at least one stop.
    - Test that every stop has title, target type, why-it-matters guidance, and a direct href/target reference.
    - Test that stop target references resolve to known corpus, chapter, concept, formal object, citation, or reading-path IDs.
  </behavior>
  <action>Create `site/src/data/discovery.schema.ts` with Zod schemas and inferred types for discovery target references, reading-path stops, branches, and path records. Create `site/src/data/reading-paths.ts` with curated records for the five required paths per D-04; derive or validate hrefs from existing registries rather than hardcoding invalid links. Add `site/src/data/discovery.test.ts` before implementation assertions, then make them pass. Use Phase 2 scholarly-atlas language per D-01 and route branches per D-02. Each stop must include `why` guidance and a direct target link per D-03. Do not generate these paths from relation metadata and do not add new dependencies.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/data/discovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `readingPaths` exports exactly five records for the required BOOK-04 themes.
    - Every reading path has multiple branches and route stops with `why` text and direct links.
    - Tests fail if a reading path target ID does not resolve to an existing registry/corpus item.
    - No canonical files under `science/` or `pillars/` are modified.
  </acceptance_criteria>
  <done>
    - `readingPaths` exports exactly five records for the required BOOK-04 themes.
    - Every reading path has multiple branches and route stops with `why` text and direct links.
    - Tests fail if a reading path target ID does not resolve to an existing registry/corpus item.
    - No canonical files under `science/` or `pillars/` are modified.

  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render reading paths as accessible static route maps</name>
  <files>site/src/components/discovery/ReadingPathMap.astro, site/src/pages/reading-paths/[slug].astro, site/src/styles/atlas.css, site/src/data/discovery.test.ts</files>
  <read_first>
    - site/src/components/discovery/ReadingPathMap.astro if it exists; otherwise read site/src/components/AtlasConstellation.astro for atlas-map structure.
    - site/src/pages/reading-paths/[slug].astro if it exists; otherwise read site/src/pages/corpus/[slug].astro for static route and `data-pagefind-body` patterns.
    - site/src/styles/atlas.css for existing atlas tokens and focus styles.
    - site/src/data/reading-paths.ts and site/src/data/discovery.schema.ts from Task 1.
    - .planning/phases/04-local-discovery-and-cross-corpus-exploration/04-UI-SPEC.md lines covering copy, typography, spacing, reading paths, and accessibility.
  </read_first>
  <behavior>
    - Test/source assertion that the route page uses `getStaticPaths()` over `readingPaths`.
    - Test/source assertion that searchable route-map content includes `data-pagefind-body`.
    - Test/source assertion that route stops include `Why this stop matters` and `Open stop` copy.
    - Test/source assertion that decorative connector SVG, if used, is `aria-hidden="true"` and `focusable="false"`.
  </behavior>
  <action>Create a `components/discovery` directory if needed. Implement `ReadingPathMap.astro` as semantic HTML using `nav`, `ol`, branch headings, route-stop cards, direct links labeled `Open stop`, and visible `Why this stop matters` text per D-01 through D-03. Implement `site/src/pages/reading-paths/[slug].astro` with `getStaticPaths()` from `readingPaths`, page eyebrow `Reading path`, one-sentence purpose, route map, and `data-pagefind-body` so paths enter local search per DISC-02. Extend `atlas.css` only with discovery/reading-path classes using UI-SPEC colors, typography roles, spacing tokens, focus outlines, 44px targets, and mobile collapse at max-width 720px. Do not replace BookFooterNav or existing source-trail affordances.</action>
  <verify>
    <automated>cd /home/prannayag/harness_eng/site && bun test src/data/discovery.test.ts && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `/reading-paths/building-a-harness/` and the other four path URLs are generated by the static build.
    - Route stops are keyboard-reachable links with visible labels, not JS-only canvas nodes.
    - Branch labels and target links remain visible in one-column mobile layout.
    - Pagefind-scoped content exists for reading-path pages without adding a hosted search dependency.
  </acceptance_criteria>
  <done>
    - `/reading-paths/building-a-harness/` and the other four path URLs are generated by the static build.
    - Route stops are keyboard-reachable links with visible labels, not JS-only canvas nodes.
    - Branch labels and target links remain visible in one-column mobile layout.
    - Pagefind-scoped content exists for reading-path pages without adding a hosted search dependency.

  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated data -> static pages | Structured reading-path records become public HTML links and search-indexed text. |
| Static page -> external reader | Reader follows generated links and anchors without runtime authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `site/src/data/reading-paths.ts` target refs | mitigate | Validate path stop targets against corpus/formal/concept/citation registries in tests and later validator; fail on missing IDs. |
| T-04-02 | Information Disclosure | Reading-path source links | accept | Site is public static research content; no private data or accounts are introduced. |
| T-04-03 | Denial of Service | Route map rendering | mitigate | Use static HTML/CSS only; no runtime graph/database or heavy client engine per D-14. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; if executor attempts one, stop and require package legitimacy review. |
</threat_model>

<verification>
Run `cd /home/prannayag/harness_eng/site && bun test src/data/discovery.test.ts && bun run build`. Confirm build output contains five `/reading-paths/<slug>/` pages and no modified files outside `site/` plus planning summaries.
</verification>

<success_criteria>
BOOK-04 has a working vertical slice: a researcher can open each required reading-path page, see a branching route map, understand why each stop matters, and follow direct links into existing corpus/formal/concept/citation anchors.
</success_criteria>

<output>
Create `.planning/phases/04-local-discovery-and-cross-corpus-exploration/04-01-SUMMARY.md` when done.
</output>
